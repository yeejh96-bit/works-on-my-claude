# 다른 PC에서 이어 개발하기 (개발 환경 준비)

이 문서는 **이 저장소를 다른 컴퓨터(예: 새 Ubuntu PC)로 옮겨 개발을 이어갈 때** 보는 것이다.
플러그인을 그냥 **쓰기만** 할 거라면 이 문서가 아니라 `README.md` 의 「설치」 절을 보면 된다.

---

## 1. 미리 깔려 있어야 하는 것

- **git** — 저장소를 받아오고 변경을 기록하는 도구.
- **Python 3.9 이상** — `scripts/` 안의 검사 스크립트 2개를 돌린다. (개발한 환경은 3.14.2)
- **Node.js 18 이상** — 터미널 하단 상태줄 `.claude/statusline.js` 를 돌린다. (개발한 환경은 v24.15.0)
- **Claude Code** — 이 저장소가 만드는 플러그인이 돌아가는 곳.

Ubuntu에서 앞의 셋을 한 번에 까는 명령:

```
sudo apt install git python3 nodejs
```
→ git · 파이썬 · Node.js 를 시스템에 설치한다.

**왜 3.9 / 18 이면 충분한가.** `scripts/bump-version.py` 와 `scripts/check-sync.py` 는 파이썬에 기본으로 딸려 오는
표준 라이브러리(`json` · `re` · `sys` · `pathlib`)만 쓴다. 밖에서 받아오는 라이브러리가 없다.
`.claude/statusline.js` 도 Node에 기본으로 딸려 오는 것만 쓴다.
버전이 더 높아도 상관없다 — 위 숫자는 하한이다.

---

## 2. 설치할 의존성은 없다

이 저장소에는 **밖에서 받아오는 패키지가 하나도 없다.** 그래서 이런 것을 할 필요가 없다.

- `npm install` — 할 필요 없다. `package.json` 자체가 없다.
- `pip install` — 할 필요 없다. `requirements.txt` · `pyproject.toml` 자체가 없다.
- 가상환경(파이썬 패키지를 프로젝트별로 따로 담는 상자) — 만들 필요 없다.
  굳이 쓰고 싶으면 `python3 -m venv .venv` 로 만들 수는 있지만, **그 안에 설치할 것이 없다.**

받아온 다음 바로 개발을 시작하면 된다.

---

## 3. 받아온 직후 반드시 한 번 해야 하는 것

```
git config core.hooksPath .githooks
```
→ 커밋할 때 자동으로 돌 검사 스크립트가 어디 있는지 git에게 알려 준다.

**이걸 왜 꼭 해야 하나.** 이 설정은 내 컴퓨터의 git에만 저장되는 것이라, 저장소를 받아와도 **따라오지 않는다.**
켜지 않으면 커밋 전 정합성 검사(`scripts/check-sync.py`)가 돌지 않아, 파일끼리 어긋난 채로 커밋이 그냥 통과한다.
받아온 폴더 안에서 **한 번만** 실행하면 그다음부터는 계속 적용된다.

---

## 4. git에 안 올라가는 것 — 옮기려면 손으로 옮긴다

`.gitignore` 에 적혀 있어 저장소에 포함되지 않는 것들이다. 셋 다 **없어도 개발에는 지장이 없다.**

- **`.claude/settings.local.json`** — 내 계정에서만 쓰는 「이 명령은 물어보지 말고 그냥 허용」 목록.
  없으면 승인 창이 좀 더 자주 뜰 뿐, 동작은 똑같다.
  윈도우에서 쓰던 파일을 그대로 가져오면 안에 적힌 `PowerShell(...)` 이나 `py` 표기가 리눅스에서는 맞지 않는다 —
  각각 `Bash(...)` 와 `python3` 로 고쳐서 쓴다.
- **`reference/`** — 외부에서 받아 둔 참고 자료 폴더. 개발에 필요하지 않다.
- **비밀키** — 옮길 것이 없다. 이 저장소에는 `.env` 파일 자체가 없다.

---

## 5. 제대로 옮겨졌는지 확인하기

```
python3 scripts/check-sync.py
```
→ 저장소 안의 파일들이 서로 어긋나지 않았는지 검사한다. 마지막 줄이 통과로 끝나면 정상이다.
(어긋난 곳이 있으면 `DRIFT` 로 표시되고 종료코드 1 로 끝난다.)

```
node .claude/statusline.js
```
→ 상태줄 스크립트가 에러 없이 실행되는지 본다. Node가 제대로 깔렸는지 확인하는 용도다.

---

## 6. 개발 서버는 없다

이 저장소는 **띄워 놓고 접속하는 앱이 아니라 Claude Code 플러그인**이다. `npm start` 같은 것이 없다.
고친 결과를 실제로 확인하려면 둘 중 하나를 한다.

- Claude Code 안에서 `/plugin marketplace add <이 저장소를 받아온 폴더 경로>` 로 **지금 이 폴더를 직접** 붙여 쓴다.
  (GitHub 대신 로컬 폴더를 가리키는 것이다. 설치·설치 후 절차는 `README.md` 의 「설치」 절과 같다.)
- 또는 아무것도 없는 빈 폴더를 하나 만들어 그 안에서 Claude Code를 켜고 `/womc` 를 돌려 본다.

---

## 7. Docker는 쓰지 않는다

이 저장소에 `Dockerfile` 은 없고, 컨테이너로 돌릴 일도 없다.
