# 작업 규칙
<!-- womc:skeleton-version=4.2.1 -->

이 파일은 매 세션 자동으로 읽힌다. 「작업 규칙」은 womc 가 관리하고 「브리핑」은 모델이 채운다. 전체 200줄을 넘기지 않는다.

## 나에 대해
- 나는 코딩을 모른다. 전문 용어는 풀어서 설명한다.
- 모든 설명·보고는 한국어로 한다.
- 답은 결론부터, 대개 다섯 줄 안. 인사말·완충어·도구 중계·꾸밈용 표·이모지는 없앤다.
- 뜻이 상하면 안 줄인다. 부정어·숫자·코드·명령·에러 문구는 그대로.
- 채팅 밖으로 나가는 글(커밋·문서)은 평문이다.

## 일하는 방식
- 되묻기·계획·구현·검증은 네가 맡는다. 여기 없는 것은 네가 정하고 한 줄로 알린다.
- 되돌릴 수 있는 일은 묻지 않는다. 손대기 전에 체크포인트 커밋(git add · git commit)을 남기고 진행한다. push 는 하지 않는다.
- 커밋으로도 못 되돌리는 것만 하기 전에 한 줄로 묻는다. 예: git push / 배포·외부로 보내기 / 데이터베이스 내용 지우기 / git reset --hard / 파일·폴더 지우기(체크포인트 커밋이 있고 git 이 추적하는 파일은 예외) / 폴더 밖으로 옮기기. 무엇이 사라지는지 쉬운 말로 함께 알린다.
- git 저장소가 아니면 묻지 않고 git init 부터 한다.
- 물어야 할 때는 선택지로 묻는다(AskUserQuestion). 답이 달라도 결과가 크게 안 달라지면 묻지 않고 정한다.
- 한 번에 한 기능만 한다. 대화가 무거워지면 /compact 나 새 세션을 제안한다.

## 기능 작업 순서 (요청 하나에 한 번 돈다. 단계·세부 단계마다 다시 돌지 않는다)
1. 요청이 모호하면 완전히 이해될 때까지 선택지로 되묻는다. 답이 달라도 결과가 크게 안 달라지면 묻지 않는다. 끝나면 「무엇을 만들고 · 무엇은 안 만들고 · 어떻게 확인할지」를 쉬운 말로 다섯 줄 안에 보여 주고 시작한다. 앞으로도 지킬 것은 브리핑에 적는다.
2. 일을 단계로 쪼개 채팅에 보여 준다. 각 단계는 그 줄만 보고 대화 없이 이어 할 수 있게 적는다. 시작 전에 「무엇이 깨질 수 있나」를 한 번 스스로 묻는다. TASKS.md 에는 이번 세션에 못 끝내는 단계만 적고, 끝난 단계는 적지 않는다.
3. 체크포인트 커밋.
4. 구현. 단계마다는 확인 명령을 네가 직접 돌려 결과를 보고 넘어간다(여기서 검증 서브에이전트를 부르지 않는다). 무거운 조사·구현은 서브에이전트에 맡기고 결론만 받는다.
5. 검증 서브에이전트는 요청 하나에 딱 한 번, 사용자에게 「됐다」고 말하기 직전에만 부른다. 브리핑의 확인 방법을 그 서브에이전트가 실행한다(만든 쪽이 스스로 판정하지 않는다). 단계·세부 단계(1-1, 1-2 …)가 끝난 자리에서는 부르지 않는다.
6. 실행 결과·화면(로그 요지·스크린샷)을 붙여 한 줄로 보고하고 커밋한다.

## TASKS.md (필요할 때만)
- 한 세션에 안 끝날 일에만 만든다. 항목마다 「손댈 파일 · 이어 쓸 것 · 끝난 조건 · 확인 방법」 네 줄. 네 줄만 보고 대화 없이 이어 할 수 있어야 한다.
- 끝난 항목은 지운다. 파일이 비면 파일도 지운다. 끝난 기록은 커밋 메시지가 맡는다.

## 규칙·스킬 남기기
- 앞으로 계속 지킬 제약은 .claude/rules/<이름>.md 에 남기되 반드시 paths 를 붙인다(그 파일을 만질 때만 읽히게). 전체에 걸치는 규칙은 브리핑에 한 줄로.
- 같은 절차를 두 번 이상 하면 .claude/skills/<이름>/SKILL.md 로 남긴다.
- 작업 시작 때 부푼 것(끝난 항목이 남은 TASKS.md · paths 없는 규칙 · 200줄 넘는 이 파일)이 보이면 묻지 않고 정리하고 한 줄 보고한다.

## 브리핑 (모델이 채우고 고친다)
코드로 알 수 없는 것만 적는다. 보통 「할 일 · 지킬 것 · 끝난 조건 · 확인 방법(빌드·테스트·실행 명령)」이 들어가지만 필요한 것만 적고 없는 칸은 만들지 않는다.
사용자가 한 말 중 앞으로도 지킬 것은 여기로 옮긴다. 브리핑이 바뀌면 .claude/rules 와 .claude/settings.json 도 그에 맞춘다.

### 할 일
이 저장소는 Claude Code 플러그인 womc 자체다. 새·기존 프로젝트에 얇은 기본 하네스를 깔아, 코딩을 모르는 사용자가 무엇을 원하는지만 말하면 Claude Code 가 되묻기·계획·구현·검증을 맡아 꾸준히 잘 일하게 만드는 것이 목표.

### 지킬 것
- 철학: 사람은 무엇을 원하는지와 되돌릴 수 없는 결정만 맡는다. 되묻기·계획·구현·검증은 모델이 맡는다. 그래서 하네스는 얇고, 모델이 좋아질수록 더 얇아진다.
- 정본은 `skills/womc/SKILL.md` 하나다. 이 저장소의 `CLAUDE.md`·`.claude/settings.json`·`templates/statusline.js` 는 그 사본이라 정본을 고치면 같이 고친다.
- 안 만들 것: `.mcp.json` 자동 생성, 기술 스택·기능을 골격에 미리 박기, `/womc` 가 코드 구현하기, GUI, 플러그인이 주는 서브에이전트·절차 스킬(내장 에이전트로 충분하다).
- 권한 규칙에 `Write(경로)` 를 쓰지 않는다. 경로를 붙일 때는 `Edit(경로)` 로 적는다.
- 버전을 올릴 때는 `python3 scripts/bump-version.py <새버전>` 으로 한 번에 올린다(plugin.json · README 제목 · 골격 표식).
- 배포: GitHub `yeejh96-bit/works-on-my-claude` 에 push 하면 사용자가 `/plugin` 으로 설치한다. push 는 사용자가 시킬 때만.

### 확인 방법
- `node scripts/womc-check.js` 를 이 폴더에서 돌리면 아무것도 출력하지 않아야 한다(골격이 최신). 빈 폴더에서 돌리면 「세팅이 없다」 한 줄이 나와야 한다.
- `node templates/statusline.js < /dev/null` 이 오류 없이 끝나야 한다.
- 빈 폴더에서 `claude --plugin-dir <이 폴더>` 로 세션을 열면 첫 대화에서 묻지 않고 파일 4개가 생겨야 한다.
