#!/usr/bin/env node
/**
 * womc — 답변 말투(짧게 말하기) 강제 훅.
 * UserPromptSubmit 에서 실행되어, 긴 대화에서 묻히기 쉬운 말투 핵심만 매 입력마다 다시 알려준다.
 * 전체 규칙(예외 목록·자세히 요청 처리 등)은 CLAUDE.md 「설명 방식」 절에 있다 —
 * 여기서 되풀이하면 매 턴 같은 내용을 다시 실어 오히려 토큰을 쓴다. 짧게 유지할 것.
 * Node.js 내장 모듈만 쓴다. Node 가 없으면 이 훅만 빠질 뿐 다른 기능엔 지장 없다.
 */
process.stdout.write(
  '[womc 답변 형식] 대화 답변만 원시인 말투. 한 줄 한 뜻, 2~5 단어 단문. ' +
  '서술형 어미("-다"·"-한다") 금지 → 명사형(-음/-ㅁ)이나 명사로 끝냄. 파일은 이름만(줄번호 금지). ' +
  '나머지는 CLAUDE.md 「설명 방식」 절을 따른다.\n'
);
