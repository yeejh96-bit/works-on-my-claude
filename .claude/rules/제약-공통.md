어디에나 걸리는 제약이다 — `paths` 가 없어 항상 읽힌다(늘 토큰을 쓴다).
그래서 짧게 유지한다 — 특정 파일에만 걸리는 것은 `.claude/rules/제약-<영역>.md` 로 옮긴다.

## 나중에 · 안 할 것

- **출력 스타일 파일 이름을 `womc-caveman` 그대로 두고 내용만 바꾸는 길 — 안 골랐다(2026-08-19, v2.8.0).**
  다른 프로젝트의 `settings.json` 을 건드릴 필요가 없어 가장 안전하지만, 이름(`caveman`)과 내용(쉬운 말)이
  영구히 어긋나 다음 세션이 파일을 열기 전까지 무슨 말투인지 오해한다. 사용자가 「이름도 바꿈」을 골랐고,
  이주 비용은 `/womc update` 의 자동 교정 한 줄로 흡수했다.
- **`SessionStart` 훅으로 "감사할 때 됐다" 알림** — v2.1.0 의 C안이었으나 안 넣었다. 현재 Claude Code 버전과
  마지막 감사 버전이 벌어졌을 때 한 줄 띄우는 것인데, Node.js 가 필요하고 매 세션 프로세스를 하나 더 띄운다.
  `harness-audit` 스킬을 사용자가 부르면 1단계에서 어차피 버전을 대조하므로 이득이 작다.
  넣는다면 Node.js 가 없을 때 조용히 넘어가는 것이 전제다.
- **서브에이전트 `memory: project`** — 보류. `verify` 의 기억은 `TASKS.md` 「확인 방법」과 중복돼 진실이 둘이 되고,
  `explore` 의 기억은 리팩터 뒤에도 옛 위치를 확신에 차 보고할 위험이 있다(사용자가 코딩을 몰라 그 오류를 검출할 수단이 없다).
  시도한다면 "기억에서 가져온 것은 반드시 재확인" 규칙과 함께.
- **`statusLine` 의 `refreshInterval`** — 안 넣는다. 단위가 초(최소 1)라 몇 초마다 node 프로세스를 새로 띄우게 되는데,
  womc 상태줄은 이벤트 기반 갱신으로 충분해 비용이 이득보다 크다.
- **`plan` → `design` 개명** — 안 한다. 소문자 `plan` 은 내장 `Plan` 을 오버라이드하지 않는다(실측). 이름 충돌이 없어 이득이 없다.
- **설계를 반박하는 전용 서브에이전트(5종째)** — v2.4.0 에서 **안 골랐다.** 계획을 다른 에이전트가 트집 잡게 하면
  "쓴 쪽이 자기 답을 채점"하는 문제가 구조적으로 풀리지만, 큰 작업마다 한 번씩 더 돌아 값이 비싸다.
  **계획이 틀려 되돌아간 사례가 아직 쌓이지 않았다** — 그래서 사용자가 검사자를 맡는 싼 판(A안)으로 갔다.
  **다시 열 조건: `plan-feature` 5절의 「되돌아가는 경로」(재위임 2회 상한)를 다 쓰고 3절로 되돌아간 일이 두세 번 쌓이면.**
  그때는 `explore` 를 haiku 로 두고 지켜보는 것과 같은 방식으로 판단한다.
- **승인 관문을 `CLAUDE.md` 의 새 `## ` 절로 만들기** — 안 했다. 절 제목을 늘리면 `commands/womc.md` ⓐ 병합 절차 2번의
  「골격 절 제목 8개 목록」도 같은 작업에서 고쳐야 하고, 빠뜨리면 그 절이 「사용자가 쓴 절」로 오인돼 옛 판이 영원히 남는다
  (v2.2.2 가 남긴 경고). 기존 「절차 지키기」 절 안에 불릿으로 넣어 그 위험을 피했다.
- **승인 관문을 `deny` 로 막기** — v2.5.0 에서 안 골랐다. `deny` 는 사용자가 좋다고 해도 실행이 안 돼,
  `git push` 로 배포하는 이 프로젝트의 경로 자체가 끊긴다. 규칙은 「금지」가 아니라 **「물어보고 한다」**다 — 그래서 `ask` 다.
- **`ask` 목록을 넓게 잡기**(`Bash(git:*)` 처럼) — 안 했다. `git status` 까지 매번 묻게 되고, 코딩을 모르는 사용자가
  승인 피로로 전부 허용해 버리면 **관문이 죽는다.** 좁게 유지하는 것이 관문을 살리는 조건이다.
- **`ask` 에서 파일 지우기(`rm`·`Remove-Item`)를 빼고 git 4종만 넣기** — v2.5.0 에서 **사용자가 「넣는다」를 골랐다.**
  「파일·폴더 지우기」가 관문 대상 1번인데 설정에서 빠지면 절반만 구조화된다. 거슬리면 그 두 줄만 지우면 되므로 되돌리기가 싸다.
- **`PreToolUse` 훅으로 관문 만들기** — 안 했다. Node.js 가 필요하고 매 도구 호출마다 프로세스를 띄운다
  (위 `SessionStart` 훅을 기각한 것과 같은 이유). `ask` 는 설정 몇 줄이라 훨씬 싸다.
- **`check-sync.py` 6번 검사를 안 넣고 수동 grep 유지** — 안 했다. **그 수동 확인이 이번 지적의 원인이다**
  (v2.4.0 이 "2026-08-12 일치"라고 1회성으로 기록하고 끝냈다). 다만 6번은 **글자만 보지 뜻은 못 본다** —
  양쪽을 동시에 같은 이름으로 다듬으면 통과한다. 그 한계는 스크립트 docstring 에 적어 뒀다.
- **6번 검사를 마크다운 절 구조 파싱까지 확장** — 안 했다. 파서를 들이면 스크립트가 무거워지고,
  지금 문제는 「문자열이 갈라지는 것」이라 문자열 대조로 충분하다.
- **`PLAN.md`·`TASKS.md` 템플릿을 `commands/womc.md` 로 옮기기** — 안 했다. 두 템플릿의 정본은
  `skills/plan-feature/SKILL.md` 0절이다(`commands/womc.md` 에는 없다 — 2026-08-12 조사로 확인). 옮기는 것은 이번 일과 무관해 범위만 넓힌다.

- **`AGENTS.md` 를 물어보지 않고 무조건 자동 import 하기** — v2.9.0 에서 **안 골랐다.** `AGENTS.md` 가 몇 줄인지 모르는 채
  연결하면 **매 세션 로드량이 얼마나 늘지 모른다.** womc 는 always-on 을 97줄로 재는 프로젝트다(v2.8.0 감사 실측).
  그래서 줄 수를 알려 주고 **물어본 뒤 승낙할 때만** 넣는다.
- **줄 수 임계값으로 자동 판단하기**(예: 60줄 이하면 자동 연결) — v2.9.0 에서 **안 골랐다.** 새 규칙이 하나 늘고,
  그 규칙이 틀렸을 때 코딩을 모르는 사용자가 원인을 못 찾는다. **질문 한 줄이 임계값보다 싸다.**
- **`AGENTS.md` 내용을 `CLAUDE.md` 안으로 복사하기** — v2.9.0 에서 **안 골랐다.** 원본이 바뀌면 사본이 조용히 어긋난다.
  이 저장소가 정본–사본 어긋남을 막느라 `scripts/check-sync.py` 까지 만든 것과 정반대 방향이다.
- **`@AGENTS.md` 를 `womc:begin/end` 구획 밖에 두어 갱신이 안 건드리게 하기** — v2.9.0 에서 **안 골랐다.**
  골격 `CLAUDE.md`(갱신 ⓐ 경로)에는 그 구획 자체가 없어 어차피 이월 규칙이 필요하다.
  규칙 하나면 되는데 자리를 둘로 나눌 이유가 없다.
- **`/womc` 자동 호출 억제를 `description` 문구로 하기** — v2.9.0 에서 **안 골랐다.** 산문은 유도일 뿐이다
  (v2.8.0 감사가 확인한 "Either instruction only steers Claude"). **설정으로 잡을 수 있는 것을 산문으로 잡지 않는다** —
  그래서 `disable-model-invocation: true` 다.
- **`AGENTS.md` 연결을 `open:import-command` 실측이 끝날 때까지 미루기** — v2.9.0 에서 **안 골랐다.**
  그 실측의 조건이 「다음에 기존 프로젝트에 `/womc` 를 깔 때」라 기약이 없고, `AGENTS.md` 연결은 멱등이라
  (이미 연결돼 있으면 다시 안 넣는다) 미뤄서 얻는 이득이 작다.

## 사용자가 닫은 것

- **답변 말투는 「쉬운 말 + 짧게」다(2026-08-19, v2.8.0부터).** 읽는 사람이 코딩을 모른다는 것이 기준점이다 —
  존댓말 평서형, 한 문장 한 뜻, 답변 기본 5줄 안, 전문 용어는 괄호로 풀이. 짧게 만드는 것보다 **알아듣는 것이 먼저**다.
  뜻이 흐려질 문장은 풀어 쓴다. **말투 규칙의 정본은 `output-styles/womc-plain.md` 한 곳**이다 —
  CLAUDE.md·SPEC.md·HARNESS.md·README.md 에는 **한 줄 요지와 「출력 스타일이 정한다」는 가리킴까지만** 두고
  규칙 전문을 베껴 적지 않는다. 말투를 손볼 때는 정본을 고치고, 그 네 곳의 한 줄 요지가 어긋나지 않는지만 본다.
- 서브에이전트 4종(`explore`/`plan`/`implement`/`verify`)의 보고 문체는 대화 답변 말투로 바꾸지 않는다
  (사용자가 읽는 글이 아니라 메인이 읽는 중간 산출물이므로 정확도를 우선한다).
  v2.0.0 부터는 출력 스타일이 서브에이전트에 적용되지 않아 이게 소프트 가드가 아니라 구조적으로 보장된다.
- 브레비티 강도 사다리(약하게/보통/최소)는 부활시키지 않는다 — 사용자가 명시적으로 "한 단계로만 강제"를 요청했다.
- `.claude/rules/` 에 문체 규칙 파일을 만드는 것 — 안 한다. 문체는 출력 스타일 `output-styles/womc-plain.md` 한 곳에서만 관리한다.
  (v2.8.0 이전에는 이 자리가 `output-styles/womc-caveman.md` 였다 — 파일 이름이 바뀌면 여기도 함께 고친다.)
- **계획을 검사하는 역할은 사용자가 맡는다** (v2.4.0). `plan` 이 대안과 기각 이유를 함께 내놓고, `plan-feature` 3절이
  그것을 쉬운 말로 사용자에게 보여주며, 사용자가 고른다. 서브에이전트를 늘려 검사시키지 않는다 — 위 「나중에 · 안 할 것」 참고.
- **v2.4.0 이 기대고 있는, 아직 확인 못 한 가정 2개** (틀리면 그 조항을 되돌린다):
  - **승인 관문의 범위(불릿 2개 · 대상 5가지)가 적당한지.** 넓으면 매번 걸려 귀찮아지고, 좁으면 못 막는다.
    **써 보기 전에는 모른다** — 실제로 되묻기가 잦아 거슬리면 대상을 줄인다.
  - **「안 고른 길」 되묻기를 건너뛰는 판단이 실제로 작동하는지.** 3절에 "대안이 없거나 자명하면 건너뛴다"고 적었지만,
    매번 묻는 쪽으로 기울면 작은 작업이 무거워진다.
- **승인 관문은 산문이 아니라 설정으로 보장한다** (v2.5.0). 골격 `.claude/settings.json` 의 `permissions.ask` 가 정본이고,
  `CLAUDE.md` 「절차 지키기」의 문장은 그 설정을 설명하는 자리다. **소프트 가드로 되돌리지 않는다** —
  v2.0.0 이 「소프트 가드 → 구조적 보장」으로 옮긴 것과 같은 방향이다.
- **v2.5.0 이 기대고 있는, 아직 확인 못 한 가정 3개** (틀리면 그 조항을 되돌린다 — 전문은 `TASKS.md` 의 `open:ask-gate`):
  - **`ask` 가 프로젝트 `settings.json` 에서 동작하고 `settings.local.json` 의 `allow` 를 이기는지.** 문서 기재만 보고 넣었다.
    틀리면 v2.5.0 의 핵심 변경이 통째로 헛돈다.
  - **Windows 에서 git 명령이 `Bash(...)` 로 도는지 `PowerShell(...)` 로 도는지.** 확정 못 해 둘 다 넣었다.
    둘 다 아닌 표기면 아무것도 안 잡힌다.
  - **프리픽스 매칭이 `git reset --hard` 처럼 플래그가 낀 접두사와 결합 명령(`cd x && git push`)에서도 잡히는지.**
    안 잡히면 `Bash(git reset:*)` 처럼 한 칸 넓혀야 하고, 그만큼 되묻기가 는다.
- **`AGENTS.md` 연결은 항상 「물어본 뒤에만」 한다** (v2.9.0 — 2026-08-19 사용자가 고른 길).
  **매 세션 로드량이 늘어나는 자리는 자동으로 결정하지 않는다** — 줄 수를 알려 주고 승낙을 받은 뒤에 넣는다.
  무조건 자동 import 로 되돌리지 않는다(근거는 위 「나중에 · 안 할 것」).
- **신규 프로젝트용 `CLAUDE.md` 골격 템플릿에는 `@AGENTS.md` 를 넣지 않는다** (v2.9.0).
  신규 빈 폴더에는 `AGENTS.md` 가 없고, 템플릿에 박으면 라이브 `CLAUDE.md` 와 글자가 어긋나
  `scripts/check-sync.py` 1번이 DRIFT 로 잡는다. 연결은 온보딩·갱신 경로에서만 붙인다.
- **`CLAUDE.md` 를 덮는 경로가 늘어나면 `@AGENTS.md` 이월 규칙도 함께 늘린다** (v2.9.0).
  지금은 ⓐ(절 단위 병합) · ⓑ(`womc:begin/end` 구획 교체) 두 경로 모두에 이월이 걸려 있다.
  **한쪽만 걸면 그 경로를 타는 프로젝트에서 연결이 조용히 사라진다.**
