어디에나 걸리는 제약이다 — `paths` 가 없어 항상 읽힌다(늘 토큰을 쓴다).
그래서 짧게 유지한다 — 특정 파일에만 걸리는 것은 `.claude/rules/제약-<영역>.md` 로 옮긴다.
**아래 모든 항목의 전문(경위·근거·다시 열 조건)은 `docs/CHANGELOG.md` 「닫힌 결정·안 고른 길 (전문)」에 있다.** 여기엔 결론만 둔다.

## 나중에 · 안 할 것
- 출력 스타일 파일 이름을 `womc-caveman` 그대로 두고 내용만 바꾸기(2026-08-19, v2.8.0 — 이름도 함께 바꿨다) · 서브에이전트 `memory: project` · `statusLine` 의 `refreshInterval` · `plan` → `design` 개명 — 모두 안 한다.
- 훅으로 만드는 것 — 안 한다: `SessionStart` 감사 알림(v2.1.0 C안) · `PreToolUse` 승인 관문(v2.5.0). Node.js 가 필요하고 프로세스를 매번 더 띄운다.
- 설계를 반박하는 5종째 서브에이전트 — v2.4.0 에서 안 골랐다(사용자가 검사자를 맡는 A안). 다시 열 조건: `CLAUDE.md` 의 재위임 상한(2회)을 다 쓰고 설계로 되돌아간 일이 두세 번 쌓이면.
- 승인 관문을 `CLAUDE.md` 의 새 `## ` 절로 만들기 · `deny` 로 막기 — 둘 다 안 골랐다 (v2.5.0). 기존 절 안의 불릿 + `ask` 로 갔다.
- `ask` 목록을 넓게 잡기(`Bash(git:*)` 처럼) · `ask` 에서 파일 지우기를 빼고 git 4종만 넣기 — 둘 다 안 골랐다 (v2.5.0). 좁게 + 지우기 포함이 정본이다.
- `check-sync.py` 6번 검사를 안 넣고 수동 grep 유지 · 6번을 마크다운 절 구조 파싱까지 확장 — 둘 다 안 한다(문자열 대조로 충분).
- `PLAN.md`·`TASKS.md` 템플릿을 `commands/womc.md` 로 옮기기 — 안 한다. 정본은 `skills/plan-feature/SKILL.md` 0절이다.
- `AGENTS.md` 를 묻지 않고 자동 import · 줄 수 임계값으로 자동 판단 · 내용을 `CLAUDE.md` 안으로 복사 — 셋 다 안 골랐다 (v2.9.0).
- `@AGENTS.md` 를 `womc:begin/end` 구획 밖에 두기 · 연결을 `open:import-command` 실측이 끝날 때까지 미루기 — 둘 다 안 골랐다 (v2.9.0).
- `/womc` 자동 호출 억제를 `description` 문구로 하기 — 안 골랐다 (v2.9.0). 설정(`disable-model-invocation: true`)으로 잡는다.
- `SPEC.md` 를 골격에 되돌리기 · 「프로젝트 상세」를 별도 파일로 빼 `@import` 하기 — 둘 다 안 한다 (v3.1.0). always-on 이 다시 둘이 된다.
- `/womc update` 가 옛 `SPEC.md` 를 `CLAUDE.md` 로 이관하기 — 안 한다 (v3.1.1, 2026-08-31. v3.1.0 이 넣었던 이관 절차를 통째로 뺐다). **`SPEC.md` 파일도 `@SPEC.md` import 줄도 건드리지 않고 보존만 하며, 완료 보고에서 안내 한 줄만 한다** — 정리는 사용자가 원할 때 `plan-feature` 로.
- 그 정리에서 묻지 않고 지우기 · 요약해 옮기기 — 둘 다 안 한다. 원문 그대로 옮기고, 새 자리에 들어간 것을 확인한 뒤에만 원본·연결을 지운다 (정본은 `skills/plan-feature/SKILL.md` 「문서 내용을 다른 파일로 옮길 때」).
- check-sync 1번 경계를 새 표식(`womc:skeleton:begin` 류)으로 긋기 — 안 골랐다 (v3.1.0). 기존 절 제목 `## 프로젝트 상세` 를 경계로 쓴다.
- 없앤 골격 절 2개(코드 제공 방식·설명 방식)를 한 줄로 줄여 남기기 — 안 한다 (v3.0.0, 2026-08-31). 전부 Claude Code 기본 동작이라 통째로 뺐다 — 「얇게」 철학의 첫 시험대다.
- 배포·데이터베이스 삭제를 `ask` 패턴으로 잡기 — 안 한다 (v3.0.0, 2026-08-31). 프로젝트마다 명령이 달라 누락이 확실하다 — 산문 규칙이 맡는다.
- 프로젝트 스킬 규칙을 별도 스킬(`make-skill`)로 분리 — 안 한다 (v3.0.0, 2026-08-31). 반복 감지는 항상 로드된 자리에 있어야 켜진다 — `CLAUDE.md` 절 + `make-rule` 확장으로 간다.
- 재위임 2회 상한을 `plan-feature` 에 두기 — 안 한다 (v3.0.0, 2026-08-31). 스킬은 항상 로드되지 않아 `CLAUDE.md` 로 올렸다.

## 사용자가 닫은 것
- **코드가 스펙이다** (v3.1.0, 2026-08-31). 코드와 중복되는 요약 문서를 만들지 않는다. 코드로 알 수 없는 셋(뭘 푸는가·안 만들 것·배포)만 `CLAUDE.md` 「프로젝트 상세」에 둔다. always-on 은 `CLAUDE.md` 하나다.
- **출력 스타일은 v3.0.0(2026-08-31)에서 없앴다.** 말투 규칙 파일·출력 스타일을 다시 만들지 않는다 — 말투는 `CLAUDE.md` 「나에 대해」에 맡긴다.
- 서브에이전트 4종의 보고 문체는 대화 답변 말투로 바꾸지 않는다 — 보고 문체의 정본은 `CLAUDE.md` 「서브에이전트 보고 규약」이다.
- 브레비티 강도 사다리(약하게/보통/최소)는 부활시키지 않는다 · `.claude/rules/` 에 문체 규칙 파일을 만들지 않는다.
- 위임 문턱값(「파일 3개 이상이면 위임」 같은 수치)을 골격에 다시 박지 않는다 (v3.0.0, 2026-08-31). 일을 어떻게 나눌지는 모델이 정한다.
- `harness-audit` 에 모델 변경 트리거를 다시 넣지 않는다 (v3.0.0, 2026-08-31). Claude Code 버전 축만 본다.
- 커밋은 묻지 않고 남긴다 (v3.0.0, 2026-08-31). `ask` 에는 커밋으로 못 되돌리는 것만 넣는다 — 브랜치 삭제는 커밋이 남아 되돌릴 수 있어 뺐다.
- 계획을 검사하는 역할은 사용자가 맡는다 (v2.4.0). 서브에이전트를 늘려 검사시키지 않는다 — 위 「나중에 · 안 할 것」 참고.
- 승인 관문은 산문이 아니라 설정으로 보장한다 (v2.5.0). 정본은 골격 `.claude/settings.json` 의 `permissions.ask` 다.
- 아직 확인 못 한 가정 — v2.4.0 2개(관문 범위가 적당한지 · 되묻기 건너뛰기가 작동하는지) · v2.5.0 3개(`ask` 가 `allow` 를 이기는지 · Windows git 표기 · 프리픽스 매칭). 틀리면 그 조항을 되돌린다.
- `AGENTS.md` 연결은 항상 「물어본 뒤에만」 한다 (v2.9.0). 무조건 자동 import 로 되돌리지 않는다 — 근거는 위 「나중에 · 안 할 것」.
- 신규 프로젝트용 `CLAUDE.md` 골격 템플릿에는 `@AGENTS.md` 를 넣지 않는다 · `CLAUDE.md` 를 덮는 경로가 늘어나면 `@AGENTS.md` 이월 규칙도 함께 늘린다 (v2.9.0).
