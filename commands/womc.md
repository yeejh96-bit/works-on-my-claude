---
description: 새 프로젝트 폴더에 항상 동일한 기본 골격(컨텍스트 엔지니어링 + 하네스 세팅)을 생성한다. `update` 인자를 주면 불변 골격만 최신 버전으로 갱신하고, `eject <이름>` 은 플러그인이 주는 정의를 프로젝트로 꺼낸다.
argument-hint: [update | eject <이름>]
allowed-tools: Write, Edit, Read, Glob, Task, Bash
---

<!-- womc:skeleton-version=2.2.2 -->
> 버전을 올리면 `py scripts/check-sync.py` 를 돌려 모든 표식이 `plugin.json` 과 맞는지 확인한다.
> (표식이 몇 곳인지 세지 말 것 — 검사기가 전수로 잡아 준다.)

사용자 인자: $ARGUMENTS

- 인자에 `update` 가 있으면 → 맨 아래 **갱신 모드** 절차만 수행한다. (아래 생성 절차는 건너뛴다)
- 인자가 `eject` 로 시작하면 → 맨 아래 **꺼내기 모드** 절차만 수행한다. (생성·갱신 절차는 건너뛴다)
- 인자가 없으면 → 아래 생성 절차(기본)를 수행한다.

너는 지금 현재 작업 폴더에 "프로젝트 기본 골격"을 생성하는 작업만 한다.
아래 규칙을 반드시 지켜라.

## 절대 규칙
- 아래에 적힌 파일/폴더만, 적힌 내용 그대로 생성한다. (매 프로젝트 항상 동일)
- 프로젝트마다 달라지는 것(기술 스택, 사용하는 MCP 등)은 채우지 않는다. 빈 틀만 둔다.
- `.mcp.json` 은 만들지 않는다.
- 코드 구현은 하지 않는다. 구조 생성까지만 한다. SPEC 은 신규 빈 폴더면 빈 틀만 두고, 기존 코드가 있는 폴더면 `explore` 로 훑어 **초안까지만** 채운다(사용자 검토 전제 — 아래 '기존 프로젝트 온보딩' 참고).
- 생성하기 전에 먼저 `Glob` 로 현재 폴더에 같은 이름 파일이 이미 있는지 확인한다.
  이미 있는 파일은 **덮어쓰지 말고 건너뛰며**, 어떤 파일이 이미 있었는지 마지막 안내에서 알려준다.
- 단, **기존 프로젝트 온보딩**일 때는 이미 있는 `CLAUDE.md`·`.claude/settings.json` 을 그냥 건너뛰지 않고 womc 내용을 **덧붙여 병합한다** — 사용자가 쓴 내용은 한 줄도 지우지 않고 추가만 한다(아래 '기존 프로젝트 온보딩' 참고).

## 생성할 파일과 내용

### 1) `CLAUDE.md`
아래 내용을 그대로 쓴다.

```markdown
# 작업 규칙 (모든 프로젝트 공통 · 불변)
<!-- womc:skeleton-version=2.2.2 -->

이 파일은 매 세션 자동으로 로드된다. 아래 규칙은 프로젝트와 상관없이 항상 적용된다.

## 나에 대해
- 나는 코딩을 모른다. 전문 용어를 최소화하고, 필요한 건 풀어서 설명한다.

## 코드 제공 방식
- 코드 수정은 너(Claude)가 파일에 직접 한다. 나는 복사·붙여넣기 하지 않는다. (바뀐 부분만 정확히 고친다)
- 보여줄 때는 무엇을 왜 바꿨는지 한국어로 설명하고, 필요한 만큼만 보여준다. (작은 파일이면 전체, 큰 파일이면 바뀐 부분 위주 — 토큰 낭비를 막는다)
- 어떤 파일인지 파일 경로를 함께 적는다.
- 파일을 새로 만들거나 바꾸면, 그다음 무엇을 하면 되는지 한 줄로 알려준다.

## 설명 방식
- 모든 설명·보고는 한국어로 한다.
- 대화 답변의 말투는 womc 출력 스타일(`womc-caveman`)이 정한다 — 이 절에 말투 규칙을 다시 적지 않는다. (출력 스타일은 서브에이전트에는 적용되지 않는다. 서브에이전트 보고는 아래 「서브에이전트 보고 규약」을 따른다.)

## 서브에이전트 보고 규약 (서브에이전트로 호출됐을 때만)
- 네가 서브에이전트면 아래를 따른다. **메인 대화면 이 절을 무시한다.**
- 보고는 한국어로 한다.
- 파일·로그 원문을 통째로 붙여넣지 않는다. `파일경로:줄번호` 로 위치만 가리킨다.
- 확실하지 않으면 "확인 못 함"이라고 분명히 적는다.
- 인사말·서론·되풀이 없이 불릿마다 한 줄, 명사형으로. 위 세 항목과 위험 신호는 줄이지 않는다.

## 적극 위임 (오케스트레이터 방식)
- 너(메인)는 지휘자다. 무거운 일은 서브에이전트에 맡기고 결론만 받아 조립한다. 메인 대화는 얇게 유지한다.
- **아래 셋 중 하나면 위임한다**: ① 파일 3개 이상을 뒤져야 할 때 ② 실행 로그·테스트 출력이 길게 쏟아질 때 ③ 독립적인 갈래가 둘 이상이라 병렬로 돌릴 수 있을 때. 파일 1~2개로 끝나는 일은 메인이 직접 한다 — 위임 자체가 비용이다(서브에이전트는 매번 CLAUDE.md 를 다시 읽고 파일을 다시 찾는다).
- 상태줄의 컨텍스트 사용량이 절반을 넘어가면 위임 비중을 올린다.
- 조사는 `explore`(여러 갈래면 한 메시지에서 여러 개 병렬), 설계는 `plan`, 구현은 `implement`, 동작 검증은 `verify` 에 맡긴다. 코드 검토는 서브에이전트를 쓰지 않고 사용자에게 `/code-review` 를 권한다.
- 이 4종에 딱 안 맞는 일은 내장 `general-purpose` 에이전트에 **그 자리에서 역할·지시를 담은 프롬프트로** 즉석 위임한다 — 파일을 새로 만들 필요 없이 필요한 만큼 자유롭게 띄운다.
- 코드 수정은 `implement` 가 파일에 직접 하고, 메인은 그 결과를 받아 조립한다(사용자에게 복붙 시키지 않는다는 원칙은 그대로다).
- 한 번에 한 기능만 진행한다. 큰 작업을 한꺼번에 벌이지 않는다.
- 한 기능이 끝나면 새 세션으로 시작하길 사용자에게 권한다(진행상태는 PLAN.md/TASKS.md 에 남긴다 — 아래 「세션을 이어서 하기」).
  작업 중간에 대화가 무거워지면 사용자에게 `/compact` 를 제안한다. (`/compact` 는 사용자 명령이라 네가 직접 실행할 수 없다.)

## 세션을 이어서 하기
- `PLAN.md`·`TASKS.md` 는 **필요할 때만 읽는다** — 사용자가 하던 일을 이어가자고 하거나, 기능 작업(`plan-feature`)을 시작할 때. 그냥 인사하거나 무관한 질문을 할 때는 열지 않는다(평소 컨텍스트를 가볍게 두는 게 이 두 파일을 import 하지 않는 이유다). 열었으면 「지금 하는 일」부터 이어가고, 비어 있으면 「할 일」의 맨 위 항목을 집는다.
- 각 항목에는 「손댈 파일」·「이어 쓸 것」(앞 단계가 남긴 파일·함수 이름)·「끝난 것으로 보는 조건」·「확인 방법」이 적혀 있다. **그 항목만 읽고도 일을 시작할 수 있어야 한다.** 적힌 게 부족하면 사용자에게 되묻지 말고 `explore` 로 먼저 확인한다.
- 반대로, 한 단계를 끝냈으면 **그 단계가 남긴 것(만들어진 파일 경로, 다음 단계가 쓸 함수·설정 이름)을 `TASKS.md` 에 적는다.** 이걸 빠뜨리면 다음 세션이 앞 단계의 결과물을 못 찾아 같은 것을 다시 만든다.
- **버전을 올리는 작업이면 `PLAN.md`·`TASKS.md` 도 같은 작업에서 반드시 함께 갱신한다.** (버전 = `plugin.json`·`package.json` 등의 `version` 값.) 작은 변경이라 PLAN/TASKS 절차를 건너뛴 경우에도 예외가 없다 — 버전이 올라가면 기록도 올라간다. `PLAN.md` 의 버전 이력에 「무슨 버전 — 무엇을 바꿨는지」 한 줄을 더하고, `TASKS.md` 「끝난 일」에 손댄 파일·남긴 것·확인 방법을 적는다. 두 파일이 없으면 그때 만든다.

## 절차 지키기 (벗어나면 되돌리기)
- 사용자가 "○○ 만들자 / ○○ 고쳐줘"라고 하면 `plan-feature` 스킬로 진행한다. 아주 사소한 것이 아니면 절차 없이 바로 고치지 않는다. (대화가 길어져 스킬 목록이 눈에 안 보여도 이 이름으로 부른다 — 스킬 목록은 `/compact` 뒤에 다시 주입되지 않지만 이 파일은 다시 주입된다.)
- `SPEC.md` 가 거의 비어 있는데 기능 요청이 오면, 먼저 SPEC 을 같이 채우자고 쉬운 말로 안내한다.
- `TASKS.md` 에 하는 중(`[~]`) 항목이 있는데 다른 기능 요청이 오면, 하던 것부터 끝내자고 먼저 권한다. (사용자가 그래도 새 걸 원하면 따르되, 하던 항목이 중단됐다고 `TASKS.md` 에 표시한다.)
- 검증을 건너뛰고 다음으로 가자고 하면, 방금 만든 게 실제로 동작하는지 먼저 확인하자고 한 번 권한다.

## 프로젝트 상세
이 프로젝트의 구체적인 내용(스택, 무엇을 만드는지 등)은 아래 파일에 있다.

@SPEC.md
```

### 2) `SPEC.md`
아래 내용을 그대로 쓴다. 신규 빈 폴더면 헤더만 두고 내용은 비운다.
(기존 코드가 있는 폴더면, 아래 '기존 프로젝트 온보딩'에서 `explore` 로 훑은 결과로 각 섹션을 채운다 — 초안이며 사용자가 검토·수정한다.)

```markdown
# 프로젝트 명세 (SPEC)

> 이 파일을 채운 뒤 작업을 시작한다. 비어 있으면 먼저 채운다.
> 이 파일은 CLAUDE.md 에 딸려 매 세션 로드된다. 너무 길어지지 않게 유지하고,
> 특정 영역에만 해당하는 세부 내용은 `.claude/rules/` 규칙으로 옮긴다(make-rule 스킬 참고).
> 신규 프로젝트면 각 칸을 직접 채우고, 기존 프로젝트면 /womc 가 코드에서 뽑은 초안을 검토·수정한다.

## 1. 기술 스택
<!-- 언어·프레임워크·주요 라이브러리·빌드/실행 도구 -->

## 2. 뭘 하는가
<!-- 이 프로젝트가 푸는 문제를 한두 줄로 -->

## 3. 핵심 기능 (3~5개)
<!-- 사용자가 실제로 쓰는 기능들 -->

## 4. 구조와 흐름
<!-- 주요 폴더·진입점(어디서 시작하나), 화면·데이터가 흐르는 순서 -->

## 5. 다루는 데이터
<!-- 저장하거나 주고받는 데이터, 외부 연동(DB·API·MCP 등) -->

## 6. 경계
<!-- 안 하는 것, 주의할 점, 건드리면 안 되는 부분 -->

## 7. 어디서 돌아가나 (배포)
<!-- 완성되면 어디에 올리나 — 웹 호스팅, 앱스토어, 그냥 내 컴퓨터 등. 모르면 "나중에 정함"이라고 적는다 -->
```

### 3) `HARNESS.md`
아래 내용을 그대로 쓴다.
(사람이 읽는 안내서다 — Claude가 자동으로 읽지 않으므로 평소 토큰을 쓰지 않는다.
프로젝트 소개용 `README.md` 는 만들지 않는다 — 프로젝트마다 내용이 다르므로, 사용자가 필요할 때 직접 만들거나 Claude에게 부탁한다.)

```markdown
# 하네스 안내 (HARNESS)

이 폴더는 Claude Code가 일을 잘하도록 **컨텍스트 엔지니어링**과 **하네스 세팅**을
미리 갖춘 구조다. 이 파일은 **사람(사용자)이 읽는 안내서**이며, Claude가 자동으로 읽지 않아
토큰을 쓰지 않는다.

## 먼저 — 이 골격은 womc 플러그인이 있어야 온전히 동작한다
서브에이전트·스킬·답변 말투는 파일로 깔리지 않고 **womc 플러그인이 직접 제공한다.**
다른 컴퓨터에서 이 프로젝트를 열었는데 그것들이 안 보이면 Claude Code 에서 두 줄만 실행한다.

1. `/plugin marketplace add yeejh96-bit/works-on-my-claude`
2. `/plugin install womc@works-on-my-claude`

## 깔려 있는 파일들
- **CLAUDE.md** — 프로젝트와 상관없이 항상 적용되는 불변 작업 규칙.
  Claude Code를 켜면 **매 세션 자동으로 로드**된다. (예: 한국어로 설명, 수정은 직접 하고 필요한 만큼만 보여줌)
  **서브에이전트도 이 파일을 함께 물려받는다** — 그래서 공통 규칙은 여기 한 곳에만 적는다.
- **SPEC.md** — 이 프로젝트만의 상세 내용(스택, 만들 것, 기능 등). CLAUDE.md 끝에서
  `@SPEC.md` 로 import 되어 **CLAUDE.md에 딸려 함께 로드**된다.
  신규 프로젝트면 빈 템플릿을 직접 채우고, 기존 프로젝트면 `/womc` 가 코드를 훑어 채운 초안을 검토·수정한다.
  (기존 프로젝트면 이미 있던 `CLAUDE.md`·설정에 womc 규칙을 덧붙여 병합하고, 코드 관례를 `.claude/rules/` 로 캡처하며, 구조 정리는 `plan-feature` 로 넘기도록 제안한다.)
- **.claude/settings.json** — 하네스(작업 환경) 기본값. 비밀키 파일(`.env` 와 그 변형)을 하위 폴더 것까지
  Claude가 읽지 못하게 막고(`deny`), 비밀이 없는 견본 파일(`.env.example`)은 읽을 수 있게 둔다.
  `outputStyle` 로 답변 말투를 이 프로젝트에서만 켠다. 미리 허용해 두는 명령(`allow`)은 없다 — 빈 목록으로 시작한다.
  (`git status` 같은 읽기 전용 명령은 Claude Code가 기본으로 프롬프트 없이 허용하므로 따로 적지 않는다 —
  Bash 든 PowerShell 이든 마찬가지다(Windows 실측, 2026-08-10). 차단은 파일 읽기 도구에만 걸리므로, 진짜 비밀은 프로젝트 폴더 밖에 두는 게 가장 안전하다.
  권한을 묻는 일이 잦아지면 `/fewer-permission-prompts` 를 한 번 돌리면 자주 쓰는 읽기 전용 명령이 허용 목록에 추가된다.)
- **.claude/statusline.js** — 터미널 하단 상태줄 스크립트(Node). 모델명·컨텍스트 토큰 사용량·5시간/주간 사용한도·현재 폴더명을 보여준다.
  `.claude/settings.json` 의 `statusLine` 항목이 이 파일을 실행한다. Node.js 가 설치돼 있어야 동작하며, 없어도 상태줄만 안 보일 뿐 다른 기능엔 지장 없다.
- **.claude/rules/** — 특정 작업에만 적용할 세부 규칙 자리. 여기 둔 규칙 파일은 **자동으로 인식된다** —
  맨 위 `paths` 에 적은 경로를 건드릴 때만 켜지고, `paths` 가 없으면 항상 켜진다(항상 토큰을 쓰므로 진짜 전역 규칙만).
  직접 만들 필요 없이 "앞으로 ○○하게 해줘"라고 하면 `make-rule` 스킬이 만들어 준다.
  폴더는 처음엔 없고, 첫 규칙을 만들 때 생긴다.
- **.gitignore** — git이 무시할 파일 목록(설치물, 비밀키, 빌드 산출물 등). 견본 파일(`.env.example`)은 공유해도 안전하므로 예외로 둔다.
- **HARNESS.md** — (이 파일) 사람이 읽는 구조 안내서. Claude가 자동으로 읽지 않는다.

## 플러그인이 주는 것 (파일로 깔리지 않는다)
- **서브에이전트 4종** — 무거운 일을 별도 컨텍스트에서 처리한다. 메인(지휘자)이 조사·설계·구현·검증을
  이들에게 맡기고 결론만 받아 조립하므로, 파일 내용·로그가 메인 대화에 쌓이지 않는다. 독립적인 일은 여러 개를 병렬로 돌린다.
  `explore`(조사·빠른 haiku) · `plan`(설계·품질 위주 opus) · `implement`(구현·유일하게 파일을 직접 고침) · `verify`(동작 검증).
  코드 검토는 따로 두지 않는다 — Claude Code 에 기본으로 있는 `/code-review` 를 쓴다.
  settings.json 의 비밀 `.env` 차단은 서브에이전트의 **파일 읽기 도구**에도 그대로 적용된다. 다만 Bash 를 가진 에이전트가 `cat .env` 처럼 셸로 읽는 것까지 막지는 못한다 — 진짜 비밀은 프로젝트 폴더 밖에 두는 게 안전하다.
- **스킬 3종** — `plan-feature` 는 기능을 새로 만들거나 고칠 때(버그수정·리팩터 포함) 쓴다 — 큰 작업은 작게 쪼개 계획(PLAN.md)·진행상태(TASKS.md)로 한 번에 하나씩, 작은 변경은 바로 고친 뒤 동작을 같이 확인한다.
  `make-rule` 은 "앞으로 ○○하게 해줘"라고 하면 적용 범위를 알아서 판단해 `.claude/rules/` 에 규칙 파일을 만든다.
  `harness-audit` 은 Claude Code 가 업데이트됐을 때 골격에서 뺄 수 있는 것을 가려내 `docs/HARNESS-AUDIT.md` 에 기록한다. `/womc update` 를 돌리면 이 감사가 자동으로 이어진다.
- **답변 말투** — 원시인 말투 출력 스타일(`womc-caveman`). `.claude/settings.json` 의 `outputStyle` 이 이 프로젝트에서만 켠다.
  말투가 안 켜지면 `.claude/settings.json` 의 `outputStyle` 값이 `womc:womc-caveman` 인지 확인한다 — 플러그인이 주는 스타일이라 앞에 `womc:` 가 꼭 붙고, 빼면 조용히 무시된다. 출력 스타일은 세션이 시작될 때 한 번만 읽히므로, 방금 켰다면 Claude Code 를 껐다 켜야 적용된다.
- 이 셋 중 무엇이든 이 프로젝트에서만 고쳐 쓰고 싶으면 `/womc eject <이름>` 으로 프로젝트 안에 꺼내 놓고 편집한다.

## 왜 스택·MCP 같은 건 비워 뒀나
기술 스택, 사용하는 MCP 등은 **프로젝트마다 다르다.** 기본 골격에 미리 박아두면
새 프로젝트에서 오히려 방해가 된다. 그래서 변하지 않는 규칙만 채워 두고,
변하는 것은 **빈 칸(SPEC.md)** 으로 두거나 **아예 만들지 않는다(.mcp.json 등).**
필요한 프로젝트에서 그때 직접 추가한다.

## 다음에 할 일
1. `SPEC.md` 를 확인한다 — 신규면 직접 채우고, 기존 프로젝트면 `/womc` 가 코드에서 뽑아 채운 초안을 검토·수정한다.
2. 기능을 만들거나 고칠 땐 "○○ 만들자 / ○○ 고쳐줘"라고 한다. `plan-feature` 스킬이 작업 크기를 가늠해
   큰 작업이면 계획(PLAN.md)·진행상태(TASKS.md)를 그때 만들어 한 번에 하나씩, 작은 변경이면 바로 처리하며,
   조사·설계·구현·검증을 전문 서브에이전트에 (필요하면 병렬로) 맡기고, 코드 검토는 `/code-review` 를 권한다.
3. 한 기능이 동작하는 걸 확인하면 커밋해 되돌릴 지점을 남긴다 — "저장(커밋)해 둘까요?"라고 물으면 Claude가 대신 `git add`·`git commit` 해 준다(`push` 는 자동으로 하지 않는다).

> PLAN.md·TASKS.md 는 처음엔 없다. 필요한 순간에만 생겨 평소 컨텍스트를 가볍게 둔다.
> 나중에 골격을 갱신하려면 그 프로젝트에서 `/womc update` 를 실행한다(직접 채운 파일은 보존된다).
> `/womc update` 는 캐시가 옛날이어도 GitHub 에서 최신 골격을 직접 받아 적용한다 — 인터넷이 막혀 받지 못할 때만 `/plugin` 업데이트와 재시작을 안내하고 멈춘다.
```

### 4) `.gitignore`
아래 내용을 그대로 쓴다.

```
# 의존성 설치물
node_modules/

# 환경변수 / 비밀키 (견본 .env.example 은 비밀이 없어 공유해도 안전 → 예외)
.env
.env.*
!.env.example

# 빌드 산출물
dist/
build/
out/

# 로그
*.log

# OS / 에디터
.DS_Store
Thumbs.db
.vscode/
.idea/

# Claude Code 개인용 로컬 설정 (공유 안 함)
.claude/settings.local.json
```

### 5) `.claude/settings.json` (하네스 — 권한 기본값 + 상태줄 + 답변 말투)
아래 내용을 그대로 쓴다.
(`.claude/` 아래에는 실제 동작 파일만 둔다 — 안내용 README 를 만들지 않는다.
`.claude/rules/` 폴더도 미리 만들지 않는다 — `make-rule` 스킬이 첫 규칙을 만들 때 생긴다.
서브에이전트·스킬·답변 말투는 **womc 플러그인이 직접 제공하므로 파일로 만들지 않는다.**)
- `deny` 로 비밀키 파일(`.env` 와 그 변형)을 **하위 폴더 것까지**(`**/.env`) Claude가 읽지 못하게 막는다. (.gitignore 의 비밀키 제외와 같은 정책)
  단, 비밀이 없는 견본 파일(`.env.example`)은 읽을 수 있어야 하므로 — Claude가 "어떤 설정값이 필요한지" 파악하는 데 쓴다 —
  `.env.*` 처럼 통째로 막지 않고 **비밀이 들어가는 이름만 골라서** 막는다. (deny 는 allow 보다 우선이라, 통째로 막으면 견본만 예외로 풀 방법이 없다.
  흔한 이름 기준이므로 특이한 이름의 비밀 파일까지 다 잡지는 못한다 — 진짜 중요한 비밀은 프로젝트 폴더 밖에 둔다.)
  또한 이 차단은 파일 읽기 도구(Read)에만 걸린다. 터미널 명령으로 읽는 것까지 다 막지는 못한다.
- `allow` 는 **빈 목록으로 둔다.** `git status`·`Get-ChildItem` 같은 읽기 전용 명령은 Claude Code 가
  **기본으로 프롬프트 없이 허용**하므로 적을 필요가 없다 — Bash 뿐 아니라 **PowerShell 도 마찬가지**다(Windows 실측, 2026-08-10).
  v2.1.1 까지는 PowerShell 4줄을 박아 두었으나 불필요한 것으로 확인돼 뺐다. 필요한 것이 생기면 그때 추가한다.
- `push`·`reset` 같이 되돌리기 어려운 명령은 일부러 넣지 않는다 — 그건 매번 확인받는 게 맞다.
- `statusLine` 은 터미널 하단 상태줄로 `.claude/statusline.js` 를 실행한다(프로젝트 루트 기준 상대 경로, 6번 참고). Node.js 가 없으면 상태줄만 안 보일 뿐 다른 기능엔 지장 없다.
- `outputStyle` 은 womc 플러그인이 제공하는 답변 말투를 이 프로젝트에서만 켠다. 다른 폴더에서는 평소 말투 그대로다.
  **값은 반드시 `womc:womc-caveman` 이다** — 플러그인이 주는 출력 스타일은 `플러그인이름:스타일이름` 으로 등록되고 이름을 정확일치로 찾기 때문에,
  접두 `womc:` 를 빼면 **경고 한 줄 없이 조용히 무시된다**(v2.1.1 에서 고친 버그다. 파일 이름 `output-styles/womc-caveman.md` 자체는 접두가 없다 — 헷갈리지 말 것).
  **바뀐 말투는 Claude Code 를 껐다 켜거나 `/clear` 한 뒤에 적용된다** — 출력 스타일은 세션이 시작될 때 한 번만 읽히기 때문이다.
  스타일이 안 먹으면 `/config` 의 Output style 목록에서 womc 의 원시인 말투를 직접 골라도 된다.

```json
{
  "outputStyle": "womc:womc-caveman",
  "statusLine": {
    "type": "command",
    "command": "node .claude/statusline.js"
  },
  "permissions": {
    "allow": [],
    "deny": [
      "Read(**/.env)",
      "Read(**/.env.local)",
      "Read(**/.env.*.local)",
      "Read(**/.env.development)",
      "Read(**/.env.production)",
      "Read(**/.env.test)",
      "Read(**/.env.staging)"
    ]
  }
}
```

### 6) `.claude/statusline.js` (하네스 — 상태줄, Node 필요)
아래 내용을 그대로, 한 글자도 바꾸지 않고 쓴다. Node.js 내장 모듈만 쓰고 하드코딩된 경로가 없어 어떤 프로젝트에도 그대로 쓸 수 있다.
stdin 으로 들어오는 JSON 을 읽어 모델명·컨텍스트 토큰 사용량·5시간/주간 사용한도·현재 폴더명을 한 줄로 만들어 출력한다.
Node.js 가 설치돼 있어야 동작한다 — 없으면 이 파일은 그냥 아무것도 안 찍고, 상태줄만 안 보일 뿐 다른 기능엔 지장 없다.

```javascript
#!/usr/bin/env node
// Claude Code statusline (jq-free, Node-based)
// Format: <model> │ <used>k/<ctx>k │ S:<5h>% W:<week>%

let raw = "";
process.stdin.setEncoding("utf8");
process.stdin.on("data", (c) => (raw += c));
process.stdin.on("end", () => {
  let d = {};
  try {
    d = JSON.parse(raw);
  } catch (e) {
    process.stdout.write("statusline: invalid input");
    return;
  }

  const model =
    (d.model && (d.model.display_name || d.model.id)) || "Unknown";

  // Current root folder name (basename of the working directory)
  const dir =
    (d.workspace && (d.workspace.current_dir || d.workspace.project_dir)) ||
    d.cwd ||
    "";
  const folder = dir
    ? dir.replace(/[\\/]+$/, "").split(/[\\/]/).pop()
    : "";

  const cw = d.context_window || {};
  const usedK = Math.round((cw.total_input_tokens || 0) / 1000);
  const ctxK = Math.round((cw.context_window_size || 0) / 1000);

  const rl = d.rate_limits || {};
  const five =
    rl.five_hour && rl.five_hour.used_percentage != null
      ? Math.round(rl.five_hour.used_percentage)
      : null;
  const week =
    rl.seven_day && rl.seven_day.used_percentage != null
      ? Math.round(rl.seven_day.used_percentage)
      : null;

  // green < 50, yellow < 80, red >= 80
  const colorPct = (label, val) => {
    if (val === null) return `${label}:--`;
    const code = val < 50 ? 32 : val < 80 ? 33 : 31;
    return `\x1b[${code}m${label}:${val}%\x1b[0m`;
  };

  let line = `${model} │ ${usedK}k/${ctxK}k │ ${colorPct("S", five)} ${colorPct("W", week)}`;
  if (folder) line += ` │ \x1b[36m${folder}\x1b[0m`;
  process.stdout.write(line);
});
```

## 기존 프로젝트 온보딩 (해당할 때만)
파일 생성을 마친 뒤, **폴더에 기존 코드가 있거나 이미 사용자가 쓴 `CLAUDE.md`·`.claude/settings.json` 이 있으면** 아래 온보딩을 진행한다. (신규 빈 폴더면 이 절 전체를 건너뛴다.)
아래 각 단계는 **자기 조건이 맞을 때만** 켜진다 — SPEC 초안·관례 캡처·구조 제안은 *기존 코드가 있을 때*, CLAUDE.md·설정 조율은 *그 파일이 이미 있을 때*(코드가 거의 없어도).
어떤 단계든 **기존 코드·파일 내용은 지우거나 덮어쓰지 않는다** — 추가·병합·제안만 한다.

- **기존 코드 판단**: `Glob` 로 매니페스트(`package.json`·`pyproject.toml`·`go.mod`·`Cargo.toml`·`pom.xml`·`Gemfile`·`composer.json` 등)나 소스 폴더(`src/` 등)·실제 코드 파일이 보이면 기존 프로젝트다. 방금 만든 골격 파일·`.git`·`node_modules` 는 신호에서 뺀다.

### 1) SPEC 초안 (이번에 `SPEC.md` 를 새로 만들었을 때만)
`SPEC.md` 가 이미 있었으면 이 단계는 건너뛴다.
- 조사는 `explore` 서브에이전트에 위임한다(직접 코드를 읽지 않아 이 대화가 가벼워진다). 영역이 여럿이면 한 메시지에서 병렬로 띄운다 — 예: ① 스택·빌드/실행 ② 핵심 기능·진입점 ③ 다루는 데이터·외부 연동. 각 `explore` 는 `파일경로:줄번호` 결론만 돌려준다.
- 돌아온 결론으로 **네(메인)가** `SPEC.md` 의 7섹션을 채운다. 확실치 않은 칸은 비워 두거나 "확인 필요"로 남긴다. 코드는 구현하지 않는다. (7번 배포 칸은 코드에서 알 수 없으면 "나중에 정함"으로 둔다)

### 2) 기존 `CLAUDE.md`·설정 조율 (이미 있을 때만)
프로젝트에 이미 **비-womc `CLAUDE.md`** 가 있으면(첫 줄이 골격의 `# 작업 규칙 (모든 프로젝트 공통 · 불변)` 이 아니면) 건너뛰지 말고 조율한다:
- 기존 파일을 읽고 **`Edit` 으로 맨 끝에만** 아래처럼 표시된 구획을 덧붙인다(파일을 통째로 다시 쓰지 말 것 — 사용자 내용을 실수로 바꾸는 걸 막는다). 사용자 내용은 한 줄도 바꾸지 않는다. 이미 `@SPEC.md` import 나 `womc:` 구획이 있으면 다시 넣지 않는다.
~~~markdown

<!-- womc:begin — 이 구획만 /womc update 가 관리. 위쪽 사용자 내용은 건드리지 않음 -->
<!-- womc:skeleton-version=2.2.2 -->
《여기》
<!-- womc:end -->
~~~
《여기》 자리에 넣을 것: 위 1) `CLAUDE.md` 골격의 **본문을 맨 위 H1 제목(`# 작업 규칙 …`) 한 줄만 빼고 글자 그대로** 넣는다.
`## 나에 대해` 부터 `@SPEC.md` 까지 7개 절이 전부 들어간다 — 신규 프로젝트가 받는 규칙과 똑같이 맞추기 위해서다.
요약하거나 이 안내 문장을 그대로 붙여 넣지 않는다. 넣는 것은 골격 본문뿐이다.
- 기존 `.claude/settings.json` 이 있으면 통째로 덮지 말고 **갱신 모드와 같은 방식**(사용자 항목 보존 + 이 문서의 기본 allow/deny 만 추가)으로 병합한다.
  이미 `statusLine` 항목이 있으면 그대로 두고 건드리지 않는다 — 없을 때만 이 문서의 기본 `statusLine`(`.claude/statusline.js` 실행)을 추가한다.
- 사용자의 기존 CLAUDE.md 가 언어·수정 방식 등 같은 주제를 이미 정해 뒀으면, 겹치는 부분을 마지막 안내에서 한 줄로 짚어 주되 사용자 문장은 지우지 않는다.
- 무엇을 병합했는지 마지막 안내에서 한 줄로 알린다.

### 3) 코드 관례를 규칙으로 캡처 (제안 후에만)
기존 코드에 일관된 관례(명명·폴더 역할·포매팅·테스트 방식 등)가 보이면, 앞으로 유지보수가 그걸 따르도록 `.claude/rules/` 로 캡처할지 **먼저 한 줄로 제안**한다. 좋다고 하면:
- 조사는 `explore` 에 위임해 관례를 뽑는다(`파일경로:줄번호` 근거).
- 영역이 뚜렷하면 `paths` 로 스코프한 규칙 파일을 만든다(예: 스타일 관례 → 맨 위 `paths: ["**/*.css"]`). 애매하면 만들지 않는다.
- 확실한 관례만 옮긴다 — 추측으로 규칙을 만들지 않는다. (형식은 `make-rule` 스킬과 같다.)

### 4) 구조 정리는 제안만 → `plan-feature` 로 넘김
코드·폴더 구조가 어질러져 보여도 **`/womc` 는 코드를 옮기거나 고치지 않는다.** 마지막 안내에서 한 줄로 제안만 한다:
- 예: "구조를 정리하고 싶으면 '구조 정리해줘'라고 하세요 — `plan-feature` 가 단계별로, 되돌릴 수 있게(커밋) 진행합니다."
- 사용자가 원하면 그때 `plan-feature` 리팩터 경로로 넘어간다. `/womc` 는 감지·제안까지만 한다.

온보딩을 마치면 무엇을 했는지(SPEC 초안·병합·캡처·구조 제안 중 실제로 한 것)를 한국어로 한 줄씩 알리고 "맞는지 검토하고 고쳐 주세요"로 맺는다.

## 생성이 끝나면
먼저, 골격 생성을 모두 마친 뒤 마무리 안내 맨 앞에 아래 WOMC 로고 아트를 코드블록(```)으로 감싸 이 모양 그대로 한 번 출력한다.
(일부 파일을 건너뛴 경우에도 아트는 동일하게 맨 앞에 한 번 넣는다.) 아트와 태그라인을 출력하고 한 줄 비운 뒤, 아래 한국어 안내로 이어간다.

```
 __        _____  __  __  ____
 \ \      / / _ \|  \/  |/ ___|
  \ \ /\ / / | | | |\/| | |
   \ V  V /| |_| | |  | | |___
    \_/\_/  \___/|_|  |_|\____|

           works on my claude
```

그다음 한국어로 짧게 다음 단계를 안내한다. **신규 빈 폴더였는지, 기존 프로젝트였는지에 따라 1)번을 다르게** 안내한다. 예시:

> ✅ 기본 골격을 만들었어요.
> 다음 순서로 진행하면 됩니다.
> 1) `SPEC.md` — **신규**면 어떤 걸 만들지 채우고, **기존 프로젝트**면 방금 코드에서 뽑아 채운 초안을 검토·수정하세요.
> 2) 기능을 만들거나 고칠 땐 그냥 "○○ 만들자 / ○○ 고쳐줘"라고 하세요. `plan-feature` 스킬이 켜져
>    큰 작업이면 계획(PLAN.md)·진행상태(TASKS.md)를 **그때 자동으로** 만들어 한 번에 하나씩 구현하고, 작은 수정이면 바로 고친 뒤 동작을 같이 확인합니다.
>
> ※ 각 파일이 무슨 역할인지는 `HARNESS.md` 에 적어 뒀어요(사람용 안내서 — Claude가 자동으로 읽지 않습니다).
> ※ PLAN.md·TASKS.md 는 지금 만들지 않습니다. 필요한 순간에만 생겨 평소 컨텍스트를 가볍게 둡니다.
> ※ 기존 프로젝트에 깐 경우, 구조가 어질러져 보이면 "구조 정리해줘"라고 하세요 — `plan-feature` 가 단계별로, 되돌릴 수 있게(커밋) 정리합니다.
> ※ MCP가 필요한 프로젝트라면 그때 `.mcp.json` 을 직접 추가하세요.
> ※ 터미널 하단 상태줄(모델명·토큰·5시간/주간 한도·폴더명 표시)은 Node.js 가 설치돼 있어야 보여요. 없어도 다른 기능엔 지장 없습니다.
> ※ `.claude/` 폴더는 Claude Code 가 보호하는 자리라, 여기에 파일을 쓸 때는 권한 모드와 상관없이 **매번 물어봅니다** — 허용을 누르시면 됩니다.
> ※ 답변 말투(원시인 말투)는 **Claude Code 를 껐다 켠 뒤부터** 적용돼요. 출력 스타일은 세션이 시작될 때 한 번만 읽히기 때문입니다.

안내를 내보내기 전에 **이 폴더가 git 저장소인지 확인한다**(`git rev-parse --git-dir`). 저장소가 아니면 위 안내 끝에 한 줄 덧붙인다:
"※ 아직 git 저장소가 아니에요. `git init` 을 해 두면 기능이 하나 끝날 때마다 되돌릴 지점(커밋)을 남길 수 있어요 — 해 드릴까요?"
(묻기만 한다. 사용자가 좋다고 할 때만 `git init` 을 실행한다. 저장소가 없으면 `plan-feature` 의 커밋 권유가 전부 실패하므로 이 확인을 건너뛰지 않는다.)

(코드를 짜지 말 것. SPEC 도 대신 채우지 않는다 — 단, 기존 프로젝트면 위의 '기존 프로젝트 온보딩'까지는 한다. 코드 구조를 직접 옮기지도 않는다 — 필요하면 제안만 하고 멈춘다.)

## 갱신 모드 (`/womc update`)
인자에 `update` 가 있을 때만 이 절차를 수행한다. 원칙: **사용자가 채운 내용은 절대 건드리지 않고, 불변 골격만 이 문서의 최신 내용으로 교체한다.**

0. **먼저 플러그인 자체를 최신으로 올린다.** (`/womc update` 한 번으로 플러그인 갱신까지 끝나게 하기 위해서다.)
   - Bash 로 실행한다: `claude plugin update womc@works-on-my-claude`
   - 성공하면 설치 캐시가 최신 버전으로 바뀐다. 다만 **지금 이 세션은 이미 읽어 둔 옛 명령 파일을 계속 쓴다** — 그래서 이번 실행분은 아래 0-b 에서 최신 문서를 직접 받아 적용한다. 캐시 갱신 효과는 다음 세션부터 나타난다.
   - 실패해도(CLI 없음·네트워크 차단·권한 거부) 멈추지 않는다. 한 줄만 알리고 0-b 로 넘어간다.
   - 무엇이 어떻게 됐는지(이미 최신이었는지 / 어느 버전으로 올랐는지 / 실패했는지)는 마지막 완료 보고에 한 줄로 적는다.
   - 참고: 이 지시는 1.15.0 부터 들어갔다. 그보다 옛 캐시로 도는 세션은 이 단계를 읽지 못하므로 플러그인이 자동으로 갱신되지 않는다 — 그때는 0-b 가 GitHub 최신 골격을 받아 적용하고, 사용자에게 `/plugin` 수동 업데이트를 안내한다.

0-b. **최신 골격 문서를 직접 받아 온다. 지금 실행 중인 이 문서가 옛 캐시일 수 있기 때문이다.**
   - 받을 곳(womc 공식 저장소 — 이 URL 을 다른 것으로 바꾸지 않는다):
     `https://raw.githubusercontent.com/yeejh96-bit/works-on-my-claude/main/commands/womc.md`
   - 받는 방법: 임시 파일로 저장한 뒤 Read 로 읽는다. (WebFetch 는 내용을 요약·변형할 수 있어 골격 원문에는 쓰지 않는다.)
     `curl -fsSL <URL> -o "${TMPDIR:-${TEMP:-/tmp}}/womc-latest.md"` — Bash 도구는 Windows 에서도 Git Bash(POSIX sh)라 이 한 줄이 모든 OS 에서 통한다.
     (`%TEMP%\...` 같은 cmd.exe 표기는 Git Bash 에서 확장되지 않아 프로젝트 폴더에 `%TEMP%\womc-latest.md` 라는 이름의 파일이 생긴다 — 쓰지 않는다.)
   - 받은 문서 맨 위의 `womc:skeleton-version=` 값이 "최신 버전", 지금 실행 중인 이 문서의 값이 "실행 중 버전"이다.
     (실행 중 문서에 표식이 아예 없으면 1.12.0 이하의 옛 캐시다.)
   - **최신 버전 > 실행 중 버전이면, 지금 이 문서의 내용은 버리고 받아 온 문서의 갱신 모드 절차와 파일 템플릿을 따른다.**
     사용자에게 한 줄 알린다. 예: "옛 캐시(1.10.0)로 실행 중이라 GitHub 에서 최신 1.14.0 골격을 받아 적용한다."
     **받아 온 문서의 0·0-b 단계는 다시 수행하지 않는다**(이미 최신을 손에 쥐었다 — 또 받으면 끝없이 반복된다). 그 문서의 1번부터 따른다.
   - 같으면 그대로 이 문서로 진행한다.
   - **다운그레이드 방지**: 프로젝트의 `CLAUDE.md` 맨 위에 `womc:skeleton-version=` 이 찍혀 있으면 그 값이 "지금 깔린 골격 버전"이다.
     적용하려는 버전이 그보다 **낮으면 아무것도 고치지 말고 멈춘 뒤** 사용자에게 알린다. 예: "지금 깔린 골격은 1.19.0 인데 적용하려는 건 1.14.0 이다. 옛 버전으로 되돌리면 손해라 멈췄다. 그래도 되돌리려면 다시 말해 달라." (표식이 없으면 1.18.0 이하이므로 그냥 진행한다.)
   - 받기에 실패하면(인터넷 차단·curl 없음) 순서대로 물러선다:
     ⓐ 로컬 마켓플레이스 클론에서 읽어 본다 — Windows `C:\Users\<사용자>\.claude\plugins\marketplaces\works-on-my-claude\commands\womc.md`, mac·linux `~/.claude/plugins/marketplaces/works-on-my-claude/commands/womc.md`
     ⓑ 그것도 안 되면, 실행 중 버전이 옛 캐시로 의심될 때(표식 없음)만 **아무것도 고치지 말고 멈춘 뒤** 이렇게 안내한다: `/plugin` 에서 works-on-my-claude → womc 업데이트 → **Claude Code 를 껐다 켠 뒤** → `/womc update` 재실행. (세션 도중 업데이트해도 그 세션은 옛 명령 파일을 계속 쓴다.)
     ⓒ 표식이 있고 최신 확인만 실패한 경우에는 "최신 여부를 확인하지 못했다"고 한 줄 알린 뒤 이 문서로 진행한다.
   - 임시 파일은 다 쓰고 지운다.
   - **0단계의 플러그인 갱신이 실패했을 때만**, 최신을 받아 썼다면 완료 보고에 이렇게 덧붙인다: 플러그인 자체는 아직 옛 버전이니 편할 때 `/plugin` 에서 한 번 업데이트해 두면 다음부터는 받아 오지 않아도 된다. (0단계가 이미 성공했다면 이 안내는 하지 않는다.)
   - 참고: 설치된 캐시 버전은 `~/.claude/plugins/installed_plugins.json` 의 `womc@works-on-my-claude` 항목(`version`·`lastUpdated`)에서도 볼 수 있다 — 사용자가 "업데이트했는데 왜 옛날 거냐"고 하면 이걸 보여주며 설명한다.

1. 아래 불변 골격 파일들을 이 문서에 적힌 내용 그대로 **덮어쓴다**(없으면 새로 만든다).

   > **덮기 전 공통 확인 — 골격이 맞는지 먼저 본다.** 아래 각 파일을 덮기 전에 기존 파일을 **먼저 읽어**, 그게 womc 가 만든 것인지 사용자가 만든 것인지 가른다.
   > 판정: 파일 첫 줄(또는 `.md` 의 frontmatter `name:`)이 이 문서의 골격 원문과 같으면 womc 것 → 덮는다.
   > **다르면 사용자 파일이다 → 덮지 말고 건너뛰고**, 완료 보고에 "○○ 는 당신이 쓴 파일로 보여 그대로 뒀다. 골격 것으로 바꾸려면 그 파일을 지우고 `/womc update` 를 다시 실행하면 된다"고 알린다.
   > (`explore`·`plan` 같은 이름은 흔해서 사용자가 이미 같은 이름으로 만들어 뒀을 수 있다. 생성 모드는 이런 파일을 건너뛰므로, 갱신 모드만 지우면 앞뒤가 안 맞고 복구도 불가능하다.)

   - `CLAUDE.md` — 세 경우로 나뉜다: ⓐ 첫 줄이 골격 그대로(`# 작업 규칙 (모든 프로젝트 공통 · 불변)`)면 **통째로 덮지 말고 절 단위로 병합한다**(바로 아래 절차). ⓑ **온보딩으로 사용자 파일에 병합된 형태(`womc:begin` 이 들어간 줄부터 `womc:end` 가 들어간 줄까지의 구획이 있는 경우)면 그 구획 안만 최신 내용으로 교체**하고 나머지 사용자 내용은 그대로 둔다. ⓒ 둘 다 아니면(사용자가 쓴 CLAUDE.md 인데 온보딩 병합을 안 거친 경우) **통째로 덮지 말고**, 위 '기존 프로젝트 온보딩'의 CLAUDE.md 병합 절차대로 `womc:begin/end` 구획을 맨 끝에 덧붙인다(사용자 내용 보존).
     ⓑ 의 구획을 찾을 때는 `<!-- womc:begin -->` 처럼 **정확히 일치하는 문자열을 찾지 않는다** — 실제 표식에는 뒤에 설명 문구가 붙어 있다. `womc:begin` 이라는 문자열이 **들어 있는 줄**을 시작으로, `womc:end` 가 들어 있는 줄을 끝으로 잡는다. (정확 일치로 찾으면 못 찾아 ⓒ 로 빠지고, 그러면 `/womc update` 를 돌릴 때마다 골격 한 벌이 계속 덧붙어 매 세션 로드량이 배로 늘어난다.)
     **ⓐ 의 절 단위 병합 절차.** 첫 줄이 골격 그대로여도 **파일 중간에 사용자가 직접 써 넣은 절이 있을 수 있다** — 실측 사례: 골격의 「절차 지키기」 절과 「프로젝트 상세」 절 사이에 그 프로젝트만의 절대 원칙 절(개인정보를 LLM 에 보내지 말 것 등)이 들어 있었다. 통째로 덮으면 그게 전부 사라진다. 그래서 이렇게 한다:
     1. 기존 파일을 읽어 `## ` 로 시작하는 제목을 경계로 절을 쪼갠다.
     2. **아래 「골격 절 제목 8개」에 해당하는 절만** 이 문서 1번 절의 `CLAUDE.md` 템플릿 내용으로 교체한다(순서도 템플릿과 같다):
        `## 나에 대해` · `## 코드 제공 방식` · `## 설명 방식` · `## 서브에이전트 보고 규약 (서브에이전트로 호출됐을 때만)` · `## 적극 위임 (오케스트레이터 방식)` · `## 세션을 이어서 하기` · `## 절차 지키기 (벗어나면 되돌리기)` · `## 프로젝트 상세`
        **템플릿의 절 제목을 바꾸면 이 목록도 같은 작업에서 함께 고친다** — 안 고치면 그 절이 「사용자가 쓴 절」로 오인돼 옛 판이 영원히 남는다.
     3. **이 목록에 없는 제목의 절은 사용자가 쓴 것이다 — 원래 순서 자리에 그대로 남긴다.** (바로 앞에 있던 골격 절 뒤에 붙여 두면 원래 자리가 유지된다.)
     4. 골격에는 있는데 기존 파일에 없는 절은 골격 순서대로 새로 넣는다.
     5. 맨 위 H1 제목 줄과 `womc:skeleton-version` 표식 줄은 최신 골격 것으로 맞춘다.
     ⓐ 로 병합했으면 **그대로 남긴 사용자 절의 제목 목록**을 완료 보고에 한 줄 적는다.
     **`## 설명 방식` 절은 따로 판정한다(ⓐ·ⓑ 모두 해당).** 덮기 전에 기존 `CLAUDE.md` 의 그 절 본문을 먼저 읽고 셋 중 하나로 가른다:
     - 절 안에 `womc:brevity=` 또는 `answer-style.js` 가 보이면 **옛 골격 판이므로 최신 것으로 교체한다.** 사용자가 고른 말투가 아니라, 이미 지워진 훅과 폐기된 강도 체계를 가리키는 죽은 문장이다(그냥 두면 파일 머리의 버전 표식만 최신이고 이 절만 옛 판으로 영영 굳는다).
     - 그 표식이 없는데 최신 골격 문구와도 다르면 **사용자가 고친 것으로 보고 원래 내용을 그대로 되돌려 놓는다.** 사용자가 말투를 바꿨을 수 있는 유일한 자리이고(`make-rule` 스킬이 문체 요청을 이 절로 보낸다), 덮어버리면 코딩을 모르는 사용자는 되돌리지 못한다.
     - 절을 아예 찾지 못했을 때만 이 문서의 기본 「설명 방식」을 쓴다.
     **교체했는지 보존했는지를 완료 보고에 한 줄 적는다.**
   - `HARNESS.md`
   - `.claude/statusline.js` — 코드 자체는 항상 최신으로 덮어쓴다(사용자가 직접 고칠 일이 없는 스크립트라서). 단, `settings.json` 의 `statusLine` 키를 켜고 끄는 건 아래 3번을 따른다.

2. **레거시 정리 — 옛 버전이 만든 골격 파일을 지운다.** (v2.0.0 부터 이것들은 womc 플러그인이 직접 제공하므로, 프로젝트에 남아 있으면 옛 정의가 플러그인 것을 가려 버린다.)
   대상: `.claude/agents/explore.md`·`plan.md`·`implement.md`·`verify.md`·`review.md` · `.claude/skills/plan-feature/` · `.claude/skills/make-rule/` · `.claude/answer-style.js`
   - 판정은 **위 1번의 「덮기 전 공통 확인」을 그대로 쓴다** — 각 파일을 먼저 읽어 womc 원본인지 사용자가 고친 것인지 가른다.
   - **womc 원본이면 지운다.** 완료 보고에 "이제 womc 플러그인이 직접 제공해서 지웠다. git 저장소라면 되돌릴 수 있다"고 한 줄 알린다.
   - **사용자가 고친 것이면 그대로 둔다** — 프로젝트 정의가 플러그인 것을 이기는 게 곧 커스터마이즈다(의도된 동작). 완료 보고에 "당신이 고친 것으로 보여 그대로 뒀다. 플러그인 기본으로 돌아가려면 그 파일을 지우면 된다"고 알린다.
   - `.claude/agents/`·`.claude/skills/` 폴더가 비게 되면 폴더도 지운다. 사용자 파일이 남아 있으면 폴더는 그대로 둔다.
   - `.claude/answer-style.js` 를 지웠으면 아래 3번에서 `settings.json` 의 그 훅 항목도 함께 뺀다.
3. `.claude/settings.json` 은 통째로 덮어쓰지 않는다. 기존 파일을 먼저 읽고,
   **사용자가 직접 추가한 항목(allow/deny 등 이 문서의 기본값에 없는 것)은 그대로 둔 채** 기본값 항목만 최신으로 맞춰 다시 쓴다.
   (기존 파일이 없으면 기본값으로 새로 만든다. 예전 기본값이던 `Read(**/.env.*)` 가 있으면 위의 새 deny 목록으로 교체한다 — 견본 `.env.example` 을 읽을 수 있게 하기 위해서다.)
   **`allow` 에 옛 골격의 PowerShell 4줄(`PowerShell(git status:*)`·`(git diff:*)`·`(git log:*)`·`(Get-ChildItem:*)`)이 있으면 그 4개만 지운다** — v2.2.0 에서 뺀 옛 기본값이다. 읽기 전용 명령은 Bash·PowerShell 모두 Claude Code 가 기본으로 프롬프트 없이 허용하므로 필요 없다(Windows 실측, 2026-08-10). **그 4개 말고 `allow` 에 들어 있는 항목은 사용자가 직접 넣은 것이므로 한 줄도 건드리지 않는다.**
   **옛 4줄을 실제로 지웠는지, 아니면 애초에 없어서 지울 게 없었는지를 완료 보고(6번)에 반드시 한 줄 적는다** — 이 청소는 한 번뿐이라 지금이 실제로 지워졌는지 확인할 유일한 기회이고, 아래 7번이 그 결과를 열린 확인 `open:allow-cleanup` 을 닫는 근거로 쓴다.
   `statusLine` 은 다르게 다룬다 — 이미 있으면(사용자가 커스텀했을 수 있으므로) **값을 덮어쓰지 않고 그대로 둔다**. 아예 없을 때만 이 문서의 기본 `statusLine`(`.claude/statusline.js` 실행)을 새로 추가한다.
   `outputStyle` 키가 없으면 `"womc:womc-caveman"` 으로 추가한다. **값이 정확히 `"womc-caveman"`(접두 `womc:` 가 빠진 옛 골격의 값)이면 `"womc:womc-caveman"` 으로 고친다** — 그 값은 어떤 스타일과도 매칭되지 않아 말투가 조용히 안 켜지던 버그이므로, 사용자가 고른 말투로 보지 않는다(v2.1.1 에서 고쳤다). 그 둘 말고 **다른 값이 들어 있으면 덮지 않고 그대로 둔다**(사용자가 고른 말투다) — 어느 쪽이었는지 완료 보고에 한 줄만 알린다.
   `hooks` 는 이렇게 다룬다 — 위 2번에서 `answer-style.js` 를 지웠으면 `node .claude/answer-style.js` 를 실행하는 `UserPromptSubmit` 항목도 함께 뺀다(스크립트가 없는데 훅만 남으면 매 입력마다 실패한다). 그 결과 `hooks` 가 비면 키째 지운다. 사용자가 직접 넣은 다른 훅은 건드리지 않는다.
4. 다음은 **절대 덮어쓰지도 지우지도 않는다**: `SPEC.md` · `PLAN.md` · `TASKS.md` · `.claude/rules/` 전체 · `.gitignore` ·
   그 외 이 문서에 없는 모든 파일. (`.gitignore` 는 사용자가 항목을 추가했을 수 있어 갱신 대상에서 뺀다.)
5. 예전 버전이 만든 `README.md` 가 있으면(첫 줄이 `# 프로젝트 구조 안내` 인 경우) 지우지 말고,
   "예전 안내 파일인데 이제 `HARNESS.md` 로 대체됐어요. 직접 지우거나 프로젝트 소개용으로 바꿔 쓰시면 됩니다"라고 안내만 한다.
   (첫 줄이 다르면 사용자가 만든 README 이므로 아무 말도 하지 않는다.)
6. 끝나면 생성 모드와 동일하게 WOMC 로고 아트를 맨 앞에 출력한 뒤, 무엇을 교체했고 무엇을 보존했는지 한국어로 짧게 알린다. **골격 버전(`womc:skeleton-version`)과 그 내용을 어디서 가져왔는지(실행 중 문서 / GitHub 최신 / 로컬 클론)도 함께 적는다.**
   **0단계의 플러그인 갱신 결과도 함께 적는다.** 새 버전으로 올랐으면 "플러그인 캐시가 최신으로 올랐다. Claude Code 를 껐다 켜야 새 명령이 적용된다"고 알린다. 이미 최신이었으면 "플러그인은 이미 최신이었다"고만 적는다. 실패했으면 실패 사실과 위 0-b 의 `/plugin` 수동 업데이트 안내를 함께 적는다.
   `.claude/` 는 Claude Code 가 보호하는 자리라 쓸 때마다 권한을 묻는다는 것, 말투는 Claude Code 를 껐다 켜야 적용된다는 것도 한 줄씩 덧붙인다.
7. **하네스 감사 (자동).** 위 1~6번으로 골격 갱신을 마쳤으면 곧바로 이어서 한다 — 사용자가 따로 부르지 않아도 된다.
   - `docs/HARNESS-AUDIT.md` 의 맨 위 기록에 적힌 「마지막 감사 기준 버전」과 지금 `claude --version` 을 비교한다. 파일이 없으면 이번이 첫 감사다 — 그냥 진행한다.
   - **두 버전이 같으면 감사 본체만 건너뛴다** — "지난 감사 이후 Claude Code 가 안 올라갔음" 한 줄을 알린다. **건너뛰더라도 `docs/HARNESS-AUDIT.md` 머리의 `womc:open-checks` 구획(`womc:open-checks:begin` 이 들어 있는 줄부터 `womc:open-checks:end` 가 들어 있는 줄까지)은 반드시 읽어, 열린 확인 항목이 있으면 한 줄씩 사용자에게 알린다.** 그 항목의 전문(조건·확인 방법)은 `TASKS.md` 「할 일」에 있으니 필요하면 거기서 읽어 온다.
     위 3번에서 옛 `allow` 4줄을 **실제로 지웠다면 그 사실이 열린 확인 `open:allow-cleanup` 의 확인 결과**이므로 함께 알리고, `TASKS.md` 의 그 항목을 닫아도 되는지 사용자에게 묻는다. (`TASKS.md` 를 말없이 고치지 않는다 — 사용자가 좋다고 할 때만 닫는다.)
   - 다르면 `harness-audit` 스킬을 그 자리에서 실행한다. (스킬 절차를 여기 베껴 적지 않는다 — 절차의 진실은 `skills/harness-audit/SKILL.md` 한 곳에만 둔다.)
   - 스킬이 「뺄 수 있음」을 하나라도 찾으면 **파일을 바로 고치지 말고 사용자에게 한 번 묻는다.** 쉬운 말 예: "지금 골격에서 빼도 되는 게 N개 나왔음. 지금 정리할까요, 기록만 남길까요?" 사용자가 좋다고 하면 `plan-feature` 로 넘어가 정리한다.
     **묻지 않고 지우는 일은 없다** — 골격을 지우는 건 되돌리기 번거롭고, 근거가 틀리면 잘 돌던 세팅이 깨지기 때문이다.
   - `harness-audit` 스킬이 없거나(플러그인 캐시가 옛 판) 실행이 안 되면 **조용히 건너뛰고** 갱신 자체는 성공으로 끝낸다 — 감사 때문에 갱신이 실패해서는 안 된다.

## 꺼내기 모드 (`/womc eject <이름>`)
인자가 `eject` 로 시작할 때만 이 절차를 수행한다. **womc 플러그인이 주는 정의를 이 프로젝트 안으로 복사해, 사용자가 마음대로 고쳐 쓰게 하는 것**이 목적이다.

꺼낼 수 있는 이름:
- 서브에이전트 — `explore` · `plan` · `implement` · `verify` → `.claude/agents/<이름>.md`
- 스킬 — `plan-feature` · `make-rule` · `harness-audit` → `.claude/skills/<이름>/SKILL.md`
- 말투 — `womc-caveman` → `.claude/output-styles/womc-caveman.md`

절차:
1. 이름을 안 줬으면 위 목록을 보여주고 무엇을 꺼낼지 한 줄로 묻는다. 목록에 없는 이름이면 목록을 보여주고 멈춘다.
2. 원본을 찾는다 — 설치된 플러그인 폴더의 `agents/`·`skills/`·`output-styles/` 아래에 같은 이름으로 있다.
   Windows `C:\Users\<사용자>\.claude\plugins\marketplaces\works-on-my-claude\`, mac·linux `~/.claude/plugins/marketplaces/works-on-my-claude/`.
   못 찾으면 GitHub 에서 받는다: `https://raw.githubusercontent.com/yeejh96-bit/works-on-my-claude/main/<원본경로>` (갱신 모드 0-b 와 같은 `curl` 방식).
3. 대상 경로에 파일이 **이미 있으면 덮어쓰지 않고 멈춘 뒤** 알린다 — 사용자가 이미 고쳐 둔 것일 수 있다.
4. 그대로 복사한다. 내용을 요약하거나 손대지 않는다.
5. 무엇을 어디로 꺼냈는지, 앞으로 어떻게 되는지 한국어로 짧게 알린다:
   - **서브에이전트·말투**: 프로젝트 파일이 플러그인 것을 **이긴다**. 이제 이 프로젝트에서는 꺼낸 파일이 쓰이니 마음대로 고치면 된다. 플러그인 기본으로 돌아가려면 꺼낸 파일을 지운다.
   - **스킬**: 다르다. 플러그인 스킬은 항상 `womc:` 이름표를 달고 있어서 **꺼내도 원본이 사라지지 않고 둘 다 살아남는다**(같은 이름 두 개가 보인다). 이 프로젝트에서 꺼낸 쪽만 쓰고 싶으면 부를 때 이름표 없는 쪽을 고르거나, 꺼낸 파일의 이름을 알아보기 쉽게 바꾼다.
6. 꺼낸 뒤에는 Claude Code 를 껐다 켜야 새 정의가 잡힌다고 한 줄 덧붙인다.
