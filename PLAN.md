# 구현 계획 (PLAN)

> "무엇을 어떤 순서로 만들지"만 적는다. 자잘한 진행 상태는 TASKS.md 가 맡는다.
> 끝난 단계의 설계 경위는 여기 쌓지 않고 `docs/CHANGELOG.md` 로 옮긴다 — 여기 남는 건 아직 안 만든 것뿐이다.
> 앞으로도 지킬 제약·안 고른 길은 여기 적지 않는다 — `.claude/rules/제약-공통.md` 에 있다.

## 만드는 순서

- 지금은 큰 덩어리 단계가 없다(유지보수 중). 다음에 할 일은 `TASKS.md` 「할 일」에 있다.
- v3.0.0 대개편(「모델을 믿는다 — 하네스는 얇게, 스킬은 두껍게」)은 2026-08-31 에 12단계 전부 끝났다.
  경위·없앤 것·안 고른 길 전문은 `docs/CHANGELOG.md` 의 v3.0.0 항목에 있다.

## 지금 집중하는 단계

(없음. 열린 확인 3건과 실사용 관찰 항목들은 `TASKS.md` 「할 일」이 정본이다.)

## 어디를 보면 되나

- 끝난 단계의 경위·이유, 버전 한 줄 요약(색인): `docs/CHANGELOG.md`
- 앞으로 지킬 제약·안 고른 길·닫힌 결정: `.claude/rules/제약-공통.md`
