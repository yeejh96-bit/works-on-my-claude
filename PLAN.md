# 구현 계획 (PLAN)

> SPEC을 "무엇을 어떤 순서로 만들지"로 옮긴 큰 그림이다.
> 자잘한 진행 상태는 TASKS.md 가 맡는다. 여기는 순서/마일스톤만.

## 만든 것 (버전 이력)

여기는 버전당 한 줄 요약만 둔다. 단계별 상세(손댈 파일·남긴 것·확인 방법)는
최근 것은 `TASKS.md` 「끝난 일」, 지난 것은 `docs/CHANGELOG.md` 에 있다.

- v1.12.0 — 케이브맨 말투를 womc 의 기본 문체로.
- v1.13.0 — 답변 말투 강제 훅(`.claude/answer-style.js`) + `/womc update` 옛 캐시 차단.
- v1.14.0 — `/womc update` 가 옛 캐시를 스스로 우회.
- v1.15.0 — `/womc update` 가 플러그인 자체도 갱신.
- v1.16.0 — 케이브맨 문장 종결을 명사형(-음/-ㅁ)으로 강제(서술형 어미 "-다"·"-이다"·"-한다" 금지).
- v1.17.0 — 케이브맨 단일 강제(약하게·보통·최소 3단계 폐지, `womc:brevity=` 표식 삭제) +
  대화 답변의 「지우지 않는 것」 목록에서 "파일 경로와 경로:줄번호" 항목 삭제(서브에이전트 보고 프로토콜은 별개라 안 건드림).
- v1.18.0 — ① 버전을 올리면 `PLAN.md`·`TASKS.md` 도 반드시 함께 갱신하는 규칙 추가(아래 「이 파일이 멈춰 있던 이유」의 재발 방지)
  ② 대화 답변에서 파일 뒤 줄번호 표기 금지(✗ `TASKS.md:10` → ✓ `TASKS.md`).
- v1.19.0 — 하네스 감사(서브에이전트 2개 병렬)에서 나온 지적 13건 일괄 수정. 상세는 `TASKS.md` 「끝난 일」.
  큰 축: `/womc update` 가 사용자 파일을 덮던 문제 3건, 저장소 안전장치 2건, 토큰 낭비 2건, 문서 사실관계 3건, 잔재·버그 3건.
- v1.20.0 — `TASKS.md` 「끝난 일」 회전 규칙을 골격에 넣음(5개 초과 시 오래된 것부터 `docs/CHANGELOG.md` 로 이동).
  이 저장소에만 적용했던 정리 방식을 `/womc` 를 쓰는 모든 프로젝트가 물려받게 했다.
- **v2.0.0 — Claude Code 2.1.x 기본기능에 맞춘 하네스 간소화.** 서브에이전트·스킬·말투를 골격이 복사하지 않고
  **플러그인이 직접 제공**하도록 옮겼다(에이전트 5종→4종, 검토는 기본 `/code-review` 로). 「서브에이전트는 CLAUDE.md 를
  못 물려받는다」는 전제가 거짓으로 판명돼 에이전트 보일러플레이트와 말투 5판본을 걷어냈다.
  `commands/womc.md` 800→495줄, 골격 생성물 11개→6개. `/womc eject <이름>` 신설, `scripts/bump-version.py` 신설.
  상세는 `TASKS.md` 「끝난 일」.

이 파일이 v1.12.0 에서 멈춰 있던 이유: v1.13~1.17 은 전부 "작은 변경"(파일 몇 개, 자명한 수정) 규모였다.
`plan-feature` 스킬 규칙상 작은 변경은 PLAN/TASKS 를 만들지 않고 바로 처리하므로,
이 두 파일이 v1.12.0 작업 이후 갱신되지 않은 채 그대로 남아 있었다(방치가 아니라 설계대로 동작한 것).
→ v1.18.0 에서 "버전을 올리는 변경은 크기와 상관없이 PLAN/TASKS 를 갱신한다"는 예외를 넣어 막았다.

## 지금 집중하는 단계
**v2.0.0 — Claude Code 2.1.x 기본기능에 맞춘 하네스 간소화. 0·1·2·3·4단계 끝(문서 3종 갱신 완료). 남은 것은 5단계(버전 2.0.0 + 실사용 검증)뿐.**
상세와 이어서 할 일은 `TASKS.md` 「지금 하는 일」, 전체 계획은 `C:\Users\s2\.claude\plans\glistening-squishing-nest.md`.

## 나중에 / 안 할 것
- **v2.1.0 — 하네스 감사 절차(`harness-audit` 스킬 + `docs/HARNESS-AUDIT.md`)**. v2.0.0 을 끝낸 뒤 시작한다.
  상세는 `TASKS.md` 「할 일」. v2.0.0 과 섞지 않기로 한 이유: v2.0.0 이 이미 파일 15개를 손대는 큰 작업이라
  섞으면 검증이 어려워지고, v2.0.0 자체가 그 감사 절차의 첫 실전 사례라 끝내고 만들어야 절차가 정확해진다.
- **서브에이전트 `memory: project`** — 보류. `verify` 의 기억은 `TASKS.md` 「확인 방법」과 중복돼 진실이 둘이 되고,
  `explore` 의 기억은 리팩터 뒤에도 옛 위치를 확신에 차 보고할 위험이 있다(사용자가 코딩을 몰라 그 오류를 검출할 수단이 없다).
  시도한다면 "기억에서 가져온 것은 반드시 재확인" 규칙과 함께.
- **`statusLine` 의 `refreshInterval`** — 안 넣는다. 단위가 초(최소 1)라 몇 초마다 node 프로세스를 새로 띄우게 되는데,
  womc 상태줄은 이벤트 기반 갱신으로 충분해 비용이 이득보다 크다.
- **`plan` → `design` 개명** — 안 한다. 소문자 `plan` 은 내장 `Plan` 을 오버라이드하지 않는다(실측). 이름 충돌이 없어 이득이 없다.

## 설계 결정 (계속 유효 — 앞으로도 지킨다)
- 서브에이전트 4종(`explore`/`plan`/`implement`/`verify`)의 보고 문체는 케이브맨으로 바꾸지 않는다
  (사용자가 읽는 글이 아니라 메인이 읽는 중간 산출물이므로 정확도를 우선한다).
  v2.0.0 부터는 출력 스타일이 서브에이전트에 적용되지 않아 이게 소프트 가드가 아니라 구조적으로 보장된다.
- 브레비티 강도 사다리(약하게/보통/최소)는 부활시키지 않는다 — 사용자가 명시적으로 "한 단계로만 강제"를 요청했다.
- `.claude/rules/` 에 문체 규칙 파일을 만드는 것 — 안 한다. 문체는 출력 스타일 `output-styles/womc-caveman.md` 한 곳에서만 관리한다.
